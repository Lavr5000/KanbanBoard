# Фича "Донаты" (Donation Feature)

## Описание
Функция для приёма донатов/пожертвований на развитие проекта Lavr Kanban AI. Предоставляет пользователям удобный интерфейс для поддержки проекта через популярные в России платёжные системы.

## Структура

```
donation/
├── model/
│   ├── types.ts              # Типы данных и константы
│   └── useDonationModal.ts   # Zustand store для управления модальным окном
├── ui/
│   ├── DonationButton.tsx    # Кнопка в сайдбаре
│   ├── DonationModal.tsx     # Модальное окно с вариантами оплаты
│   └── QRCodeDisplay.tsx     # Компонент для отображения QR-кода
├── index.ts                  # Экспорты
├── README.md                 # Документация
└── TESTING.md                # Руководство по тестированию
```

## Возможности

### Способы оплаты
1. **СБП (Система быстрых платежей)** - оплата по номеру телефона через QR
2. **ЮMoney** - перевод на счёт Яндекс.Денег
3. **Тинькофф** - перевод на банковскую карту

### Функционал
- Выбор предустановленной суммы (100₽, 300₽, 500₽, 1000₽, 2000₽, 5000₽)
- Возможность ввода кастомной суммы
- QR-код для быстрой оплаты
- Копирование реквизитов одной кнопкой
- Адаптивный дизайн для всех устройств

## Использование

### Импорт
```tsx
import { DonationButton, DonationModal } from "@/features/donation";
```

### Базовое использование
```tsx
export const Sidebar = () => {
  return (
    <aside>
      {/* Другие элементы */}
      <DonationButton />

      {/* В конце компонента */}
      <DonationModal />
    </aside>
  );
};
```

### Программное управление модальным окном
```tsx
import { useDonationModal } from "@/features/donation";

const MyComponent = () => {
  const { open, close, isOpen } = useDonationModal();

  return (
    <button onClick={open}>
      Открыть донаты
    </button>
  );
};
```

## Настройка данных для оплаты

**ВАЖНО!** Перед использованием необходимо обновить данные для оплаты в файле `model/types.ts`:

```typescript
export const PAYMENT_OPTIONS: PaymentOption[] = [
  {
    method: "sbp",
    label: "СБП",
    description: "Система быстрых платежей",
    icon: "🏦",
    data: {
      phone: "+79991234567", // 👈 ЗАМЕНИТЕ на ваш номер телефона
      qrLink: "https://qr.nspk.ru/..." // 👈 ЗАМЕНИТЕ на вашу ссылку СБП
    }
  },
  {
    method: "yoomoney",
    label: "ЮMoney",
    description: "Яндекс.Деньги",
    icon: "💳",
    data: {
      account: "410011234567890", // 👈 ЗАМЕНИТЕ на ваш кошелёк ЮMoney
      qrLink: "https://yoomoney.ru/quickpay/confirm?receiver=410011234567890&label=Donation"
    }
  },
  {
    method: "tinkoff",
    label: "Тинькофф",
    description: "Банковская карта",
    icon: "💎",
    data: {
      cardNumber: "1234 5678 9012 3456", // 👈 ЗАМЕНИТЕ на вашу карту
      qrLink: "https://www.tinkoff.ru/rm/..." // 👈 ЗАМЕНИТЕ на вашу ссылку
    }
  }
];
```

### Как получить ссылки для QR-кодов:

#### СБП (Система быстрых платежей)
1. Откройте мобильное приложение вашего банка
2. Найдите раздел "Переводы" или "СБП"
3. Создайте QR-код для получения платежей
4. Скопируйте ссылку из QR-кода

#### ЮMoney
1. Зайдите на https://yoomoney.ru/
2. Перейдите в раздел "Принимать платежи"
3. Создайте форму для оплаты
4. Получите ссылку вида: `https://yoomoney.ru/quickpay/confirm?receiver=YOUR_ACCOUNT&label=Donation`

#### Тинькофф
1. Откройте приложение Тинькофф
2. Создайте ссылку на перевод в разделе "Платежи"
3. Или используйте прямой номер карты

## Дизайн

### Цветовая схема
- **Основной градиент**: от розового (#EC4899) до фиолетового (#9333EA)
- **Фон модального окна**: #1C1C24
- **Текст**: белый (#FFFFFF) и серый (#9CA3AF)
- **Акценты**: градиенты и тени для привлечения внимания

### Принципы UX
- **Заметность**: яркая кнопка с градиентом выделяется среди других элементов
- **Ненавязчивость**: описание подчёркивает, что приложение бесплатное
- **Простота**: минимум шагов до совершения доната
- **Прозрачность**: показаны все способы оплаты и суммы

## Зависимости

- `zustand` - управление состоянием модального окна
- `lucide-react` - иконки (Heart, Copy, Check)
- `@/shared/ui/Modal` - базовый компонент модального окна

## QR-код

QR-коды генерируются через публичный API:
```
https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=YOUR_DATA
```

### Альтернативы
Если нужна локальная генерация QR-кодов, можно использовать:
- `qrcode` - популярная библиотека для генерации QR
- `qrcode.react` - React-компонент для QR
- `canvas` API - для кастомной отрисовки

## Безопасность

⚠️ **Важно**: Не храните чувствительные финансовые данные в коде!

Рекомендации:
- Используйте переменные окружения для конфиденциальных данных
- Регулярно проверяйте логи на предмет подозрительной активности
- Не храните данные о платежах на клиенте

## Производительность

- Компоненты оптимизированы для предотвращения лишних ре-рендеров
- QR-код загружается асинхронно
- Zustand обеспечивает быстрое управление состоянием
- Lazy loading для модального окна (рендерится только при открытии)

## Доступность (A11y)

- Все интерактивные элементы доступны с клавиатуры
- Контрастность соответствует стандартам WCAG
- Семантическая разметка для screen readers

## FAQ

### Как добавить новый способ оплаты?
1. Добавьте новый тип в `PaymentMethod` в `types.ts`
2. Добавьте опцию в массив `PAYMENT_OPTIONS`
3. При необходимости обновите UI в `DonationModal.tsx`

### Как изменить предустановленные суммы?
Измените массив `DONATION_AMOUNTS` в `types.ts`:
```typescript
export const DONATION_AMOUNTS = [50, 100, 200, 500, 1000];
```

### Как отключить определённый способ оплаты?
Удалите соответствующий элемент из массива `PAYMENT_OPTIONS` в `types.ts`.

### Можно ли добавить аналитику?
Да! Добавьте трекинг в `DonationButton` и `DonationModal`:
```typescript
const handleOpen = () => {
  // Ваша аналитика
  analytics.track('donation_modal_opened');
  open();
};
```

## Roadmap

- [ ] Интеграция с платёжными API для автоматической проверки платежей
- [ ] История донатов для пользователя
- [ ] Список благодарностей донатерам (с их согласия)
- [ ] Возможность оставить сообщение с донатом
- [ ] Мультиязычность (EN, RU)
- [ ] Тёмная/светлая тема
- [ ] Автоматическая генерация чеков

## Лицензия

Эта функция является частью проекта Lavr Kanban AI.

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте [TESTING.md](./TESTING.md)
2. Убедитесь, что данные оплаты настроены корректно
3. Проверьте консоль браузера на наличие ошибок
