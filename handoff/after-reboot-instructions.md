# 🔄 После перезагрузки: Восстановление и тестирование

**Дата:** 2025-12-27
**Для:** Локальный агент на ПК

---

## 📋 Что произошло

Локальный агент столкнулся с файловой блокировкой Windows (.next/trace). После перезагрузки ПК нужно восстановить проект и протестировать.

---

## ✅ Шаг 1: Очистка после перезагрузки

После перезагрузки выполни:

```bash
cd путь/к/0-KanBanDoska

# Удали папку .next (теперь должно сработать)
rm -rf .next

# Удали node_modules (если были изменения в package.json)
rm -rf node_modules

# Переключись на правильную ветку веб-агента
git checkout claude/setup-supabase-TUKCP

# Сбрось все локальные изменения
git reset --hard origin/claude/setup-supabase-TUKCP

# Установи зависимости
npm install
```

---

## ✅ Шаг 2: Проверь структуру проекта

Убедись что структура правильная:

```
0-KanBanDoska/
├── src/
│   ├── app/
│   ├── middleware.ts          ← Должен быть здесь!
│   └── providers/
├── lib/                        ← В корне проекта!
│   ├── auth.ts
│   └── supabase/
├── .env.local                  ← Твой файл с credentials
└── package.json
```

**ВАЖНО:**
- `middleware.ts` находится в `src/` - это ПРАВИЛЬНО для Next.js 15
- `lib/` в корне проекта - это ПРАВИЛЬНО
- React 19.0.0 - это ПРАВИЛЬНО для Next.js 15

**НЕ перемещай** файлы! Структура от веб-агента корректная.

---

## ✅ Шаг 3: Запусти dev сервер

```bash
npm run dev
```

Сервер должен запуститься на `http://localhost:3000`

---

## ✅ Шаг 4: Тестирование (повтори чеклист)

### Test 1: Редирект на login
- [ ] Открой http://localhost:3000
- [ ] Должен произойти редирект на `/login`
- [ ] Страница login загружается

### Test 2: Регистрация
- [ ] Нажми "Sign up" внизу
- [ ] Заполни форму (Full Name, Email, Password min 6 символов)
- [ ] После регистрации → редирект на `/`

### Test 3: UI пользователя
- [ ] В Sidebar (слева внизу) видна карточка пользователя
- [ ] Отображается: аватар, имя, email
- [ ] Кнопка "Выйти" присутствует

### Test 4: Logout
- [ ] Нажми "Выйти"
- [ ] Редирект на `/login`

### Test 5: Login
- [ ] Войди с созданным аккаунтом
- [ ] Редирект на `/`
- [ ] Карточка пользователя отображается

---

## ❌ Если ошибки

### Ошибка: "Cannot find module '@/providers/AuthProvider'"

**Причина:** Неправильная структура проекта

**Решение:**
```bash
git reset --hard origin/claude/setup-supabase-TUKCP
npm install
```

### Ошибка: "Invalid login credentials"

**Причина:** Проблема с Supabase

**Решение:**
1. Проверь `.env.local` - credentials правильные?
2. Зайди в Supabase dashboard → SQL Editor
3. Проверь что таблица `profiles` существует
4. Попробуй зарегистрировать НОВОГО пользователя

### Сервер не запускается

**Решение:**
```bash
# Убей все процессы Node
taskkill /F /IM node.exe

# Удали .next
rm -rf .next

# Перезапусти
npm run dev
```

---

## 📝 После успешного тестирования

Доложи результаты:

```
✅ Все тесты пройдены успешно!

Test 1: Редирект - OK
Test 2: Регистрация - OK
Test 3: UI - OK
Test 4: Logout - OK
Test 5: Login - OK

Скриншоты:
[если есть]

Готов к Фазе 3!
```

---

## 🎯 Важно

**НЕ изменяй:**
- package.json (React 19.0.0 правильная версия)
- Структуру папок (она правильная от веб-агента)
- Файлы в src/app/, src/providers/, lib/

**Если что-то не работает** - доложи ошибку ТОЧНО как есть, не пытайся исправить самостоятельно.

---

**Удачи!** 🚀
