# 🗺️ ДОРОЖНАЯ КАРТА: Автоматизированное тестирование Apartment Auditor

**Последнее обновление:** 2025-12-19
**Приложение:** Apartment Auditor (React Native + Expo)
**Цель:** Внедрить систему автоматизированного тестирования сценариями для ускорения разработки и снижения количества ошибок.

---

## 📊 ОБЗОР ПРОЕКТА

| Параметр | Значение |
|----------|----------|
| **Тип** | React Native + Expo 54.0.29 |
| **Основная функция** | Мобильное приложение для аудита квартир |
| **Структура** | Expo Router с табулированной навигацией |
| **State Management** | Zustand 5.0.9 |
| **Экраны** | 6+ основных экранов |
| **Модальные окна** | 5+ критических модалей |
| **Чекпоинты** | 383 чекпоинта в 9 категориях |

---

## 🎯 ЦЕЛИ И РЕЗУЛЬТАТЫ

### Ожидаемые результаты:
- ✅ Автоматизированное тестирование каждого сценария пользователя
- ✅ Сокращение времени регрессионного тестирования на 70-80%
- ✅ Раннее обнаружение критических ошибок (pre-deployment)
- ✅ Цепочка CI/CD с автоматическим запуском тестов
- ✅ Документированные тестовые сценарии для всех компонентов
- ✅ Минимум 80% покрытие кода unit/integration тестами

---

## 📋 ФАЗА 0: ПОДГОТОВКА ИНФРАСТРУКТУРЫ (1-2 дня)

### Задача 0.1: Выбор стека тестирования ✅
```
Выбранные инструменты:
├── Jest 29+ ..................... Unit & Integration тесты
├── React Native Testing Library . Component тесты
├── Detox 20+ .................... E2E тесты (native mobile)
└── Expo Config .................. Конфигурация для тестов
```

### Задача 0.2: Инициализация конфигурации
**Файлы для создания:**
- [ ] `jest.config.js` - Jest конфигурация для React Native
- [ ] `detox.config.js` - Детокс конфигурация
- [ ] `.detoxrc.json` - Дополнительная конфиг Детокса
- [ ] `app.json` - Обновить для testIDs

**Директория структура:**
```
/home/user/1-Apartment-Auditor-MVP/
├── jest.config.js
├── detox.config.js
├── tests/
│   ├── __mocks__/
│   ├── fixtures/
│   ├── setup.ts
│   ├── hooks/
│   ├── components/
│   ├── services/
│   └── integration/
├── e2e/
│   ├── scenarios/
│   ├── pages/
│   └── helpers/
├── .github/workflows/
│   ├── test-unit.yml
│   ├── test-integration.yml
│   ├── test-e2e.yml
│   └── build-and-test.yml
└── docs/
    ├── TESTING_ROADMAP.md ← вы здесь
    ├── TESTING_GUIDE.md
    ├── SCENARIO_TEMPLATE.md
    └── TEST_DATA.md
```

### Задача 0.3: Обновить package.json
**Добавить скрипты:**
```json
{
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathPattern='tests/(hooks|components|services)' --testPathIgnorePatterns=integration",
    "test:integration": "jest --testPathPattern='tests/integration'",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --coverageReporters=text-summary",
    "test:ci": "jest --ci --coverage --maxWorkers=2",
    "detox:build": "detox build-framework-cache && detox build-app --configuration ios.sim.debug",
    "detox:test": "detox test e2e --configuration ios.sim.debug --cleanup",
    "detox:test:single": "detox test e2e --configuration ios.sim.debug --grep",
    "type-check": "tsc --noEmit"
  }
}
```

### Задача 0.4: Подготовка тестовых данных
- [ ] Создать `tests/fixtures/checkpoints.fixture.ts` - примеры чекпоинтов
- [ ] Создать `tests/fixtures/projects.fixture.ts` - примеры проектов
- [ ] Создать `tests/__mocks__/asyncStorage.ts` - mock для AsyncStorage
- [ ] Создать `tests/setup.ts` - инициализация окружения

---

## 🧪 ФАЗА 1: UNIT ТЕСТЫ (3-5 дней)

### 1.1 Тесты Hooks (2 дня)

**Файлы для тестирования:**
```
tests/hooks/
├── usePhotoPicker.test.ts ............. Тесты выбора фото
├── useTheme.test.ts .................. Тесты темы
└── useCheckpointStore.test.ts ........ Тесты хранилища чекпоинтов (если есть)
```

**usePhotoPicker.test.ts - что тестировать:**
- ✓ Инициализация хука
- ✓ Запрос прав доступа к фотогалерее
- ✓ Выбор одной фотографии
- ✓ Выбор нескольких фотографий
- ✓ Отмена выбора
- ✓ Обработка ошибок (отсутствие прав)
- ✓ Возврат изображения с правильными данными

**useTheme.test.ts - что тестировать:**
- ✓ Инициализация темы (light/dark)
- ✓ Переключение темы
- ✓ Сохранение выбора
- ✓ Восстановление после перезагрузки

### 1.2 Тесты UI компонентов (2 дня)

```
tests/components/ui/
├── ProgressBar.test.tsx .............. Тесты прогресс-бара
└── (добавить другие базовые UI)
```

**ProgressBar.test.tsx - что тестировать:**
- ✓ Рендер компонента
- ✓ Отображение % прогресса
- ✓ Цветовые индикаторы (0-33%, 33-66%, 66-100%)
- ✓ Анимация заполнения
- ✓ Props обновление

### 1.3 Тесты Feature компонентов (3 дня)

```
tests/components/features/
├── CreateProjectModal.test.tsx
├── ParticipantModal.test.tsx
├── ExportProgressModal.test.tsx
├── CheckpointDetailSheet.test.tsx
├── PhotoGrid.test.tsx
├── RoomSelector.test.tsx
└── CategoryCard.test.tsx
```

**CreateProjectModal.test.tsx - что тестировать:**
- ✓ Отображение модали
- ✓ Валидация полей (название, описание)
- ✓ Заполнение формы
- ✓ Сохранение проекта
- ✓ Закрытие модали
- ✓ Обработка ошибок

**Остальные модали - аналогично**

### 1.4 Тесты Services (2 дня)

```
tests/services/
├── pdfGeneration.test.ts ............ Генерация PDF
├── reportGeneration.test.ts ........ Генерация отчетов
└── dataValidation.test.ts ......... Валидация данных
```

**Что тестировать:**
- ✓ Корректная генерация структур данных
- ✓ Обработка ошибок
- ✓ Форматирование данных

---

## 🔗 ФАЗА 2: ИНТЕГРАЦИОННЫЕ ТЕСТЫ (4-6 дней)

### 2.1 User Flow тесты (Integration)

```
tests/integration/
├── 01-project-creation.test.ts
├── 02-inspection-workflow.test.ts
├── 03-checkpoint-inspection.test.ts
├── 04-photo-management.test.ts
├── 05-participant-management.test.ts
├── 06-report-export.test.ts
├── 07-room-selection.test.ts
└── 08-category-filtering.test.ts
```

### Сценарий 01: Создание проекта
```typescript
// Файл: tests/integration/01-project-creation.test.ts
describe('Project Creation Flow', () => {
  it('should create project and save to store', async () => {
    // 1. Открыть CreateProjectModal
    // 2. Заполнить название: "Квартира на Ленина"
    // 3. Заполнить описание: "2-комнатная квартира"
    // 4. Нажать "Create"
    // 5. ✓ Проверить: Проект в хранилище
    // 6. ✓ Проверить: Модаль закрылась
    // 7. ✓ Проверить: ProjectCard отображается в списке
  });
});
```

### Сценарий 02: Полный workflow инспекции
```typescript
// Файл: tests/integration/02-inspection-workflow.test.ts
describe('Full Inspection Workflow', () => {
  it('should complete full inspection cycle', async () => {
    // 1. Выбрать проект из списка
    // 2. Выбрать категорию "Стены" (или другую)
    // 3. Для каждого чекпоинта:
    //    a. Открыть CheckpointDetailSheet
    //    b. Прочитать описание и допуски
    //    c. Выполнить проверку (pass/fail/noncompliant)
    //    d. Добавить фото (если есть)
    //    e. Добавить замечания
    //    f. Сохранить
    // 4. ✓ Проверить: Прогресс обновился
    // 5. ✓ Проверить: Все данные в store
  });
});
```

### 2.2 Store интеграционные тесты (2 дня)

```
tests/integration/store/
├── checkpointStore.integration.test.ts
├── projectStore.integration.test.ts
├── photoStore.integration.test.ts
└── reportStore.integration.test.ts
```

**Что тестировать:**
- ✓ Сохранение и загрузка данных
- ✓ Синхронизация между экранами
- ✓ Cascade updates (обновление связанных данных)
- ✓ Deletion (удаление и очистка)

### 2.3 Navigation тесты (1 день)

```
tests/integration/navigation/
├── tabNavigation.test.ts ........... Навигация между табами
└── deepLinking.test.ts ............ Глубокие ссылки
```

---

## 🎬 ФАЗА 3: E2E ТЕСТЫ (ДЕТОКС) (5-7 дней)

### 3.1 Структура E2E тестов

```
e2e/
├── scenarios/
│   ├── 01-onboarding.e2e.ts
│   ├── 02-create-project.e2e.ts
│   ├── 03-full-inspection.e2e.ts
│   ├── 04-photo-workflow.e2e.ts
│   ├── 05-export-pdf.e2e.ts
│   ├── 06-participant-workflow.e2e.ts
│   ├── 07-navigation-flows.e2e.ts
│   ├── 08-error-handling.e2e.ts
│   └── 09-accessibility.e2e.ts
├── pages/
│   ├── BasePage.ts ................. Base класс для всех экранов
│   ├── HomePage.ts
│   ├── ProjectDetailPage.ts
│   ├── InspectionPage.ts
│   ├── CheckpointPage.ts
│   └── ExportPage.ts
└── helpers/
    ├── testHelpers.ts .............. Утилиты для тестов
    ├── waitHelpers.ts .............. Ожидание элементов
    └── assertionHelpers.ts ......... Пользовательские assertions
```

### 3.2 Детальные сценарии E2E

#### Сценарий 01: Onboarding (30 мин тестирования)
```typescript
describe('Onboarding Flow', () => {
  it('should complete onboarding and navigate to home', async () => {
    // 1. Приложение запускается
    // ✓ Видим Welcome экран
    // 2. Нажимаем "Let's Start"
    // ✓ Переходим на Home экран
    // 3. ✓ Видим табуляцию (maps, objects, services)
  });
});
```

#### Сценарий 02: Создание проекта (30 мин)
```typescript
describe('Create Project Full Flow', () => {
  beforeEach(async () => {
    await device.reloadReactNative();
  });

  it('should create project with participants', async () => {
    // STEP 1: Нажать кнопку "Create New Project"
    // ✓ CreateProjectModal открылась

    // STEP 2: Заполнить название проекта
    // - Введи: "Test Apartment - Lenina St."
    // ✓ Текст отображается в поле

    // STEP 3: Заполнить описание
    // - Введи: "2-bedroom apartment"
    // ✓ Текст отображается

    // STEP 4: Нажать "Next" → ParticipantModal
    // ✓ ParticipantModal отображается

    // STEP 5: Добавить участника #1
    // - Нажать "Add Participant"
    // - Заполнить ФИ: "John Doe"
    // - Заполнить роль: "Auditor"
    // - Заполнить контакт: "john@example.com"
    // - Нажать "Save"
    // ✓ Участник добавлен

    // STEP 6: Добавить участника #2
    // - Повторить для "Jane Smith"
    // ✓ 2 участника в списке

    // STEP 7: Нажать "Create Project"
    // ✓ Модаль закрылась
    // ✓ Project появился в списке (ProjectCard)
    // ✓ Проект состояние сохранилось
  });
});
```

#### Сценарий 03: Полная инспекция (1-2 часа тестирования) ⭐ КРИТИЧНЫЙ
```typescript
describe('Complete Inspection Workflow', () => {
  it('should complete full inspection of apartment', async () => {
    // PHASE 1: ПОДГОТОВКА
    // ✓ Открыть существующий проект
    // ✓ Перейти на Object Detail экран

    // PHASE 2: ВЫБОР КАТЕГОРИИ
    // - Нажать на CategoryCard (например, "Walls")
    // ✓ Загружены все чекпоинты категории (~40 items)
    // ✓ Прогресс-бар показывает 0%

    // PHASE 3: ИНСПЕКЦИЯ ЧЕКПОИНТОВ (основной цикл)
    // FOR EACH CHECKPOINT (40+ раз):
    //   A. Нажать на CheckpointDetailSheet
    //      ✓ Sheet открывается
    //      ✓ Отображается: название, описание, допуски, метод проверки
    //
    //   B. Выполнить проверку:
    //      - Выбрать результат: "PASS" / "FAIL" / "NON-COMPLIANT"
    //      ✓ Кнопка подсвечивается
    //
    //   C. Добавить фотографии:
    //      - Нажать "Add Photo"
    //      - Выбрать из галереи 1-3 фото
    //      ✓ PhotoGrid обновляется
    //      ✓ Фото отображаются в Sheet
    //
    //   D. Добавить замечания (опционально):
    //      - Нажать "Add Note"
    //      - Введи текст замечания
    //      ✓ Замечание добавлено
    //
    //   E. Выбрать комнату (RoomSelector):
    //      - Нажать RoomSelector
    //      - Выбрать: "Living Room" / "Bedroom" / "Kitchen"
    //      ✓ Room привязана к чекпоинту
    //
    //   F. Сохранить результат:
    //      - Нажать "Save" или swipe down
    //      ✓ Sheet закрывается
    //      ✓ Прогресс-бар увеличивается на 1/40
    //      ✓ CheckpointDetailSheet исчезает из списка (если completed)

    // PHASE 4: ЗАВЕРШЕНИЕ КАТЕГОРИИ
    // ✓ Все 40 чекпоинтов проверены
    // ✓ Прогресс-бар = 100%
    // ✓ CategoryCard показывает "Completed"

    // PHASE 5: ПРОВЕРКА ДАННЫХ
    // - Вернуться в Object Detail
    // ✓ Все категории завершены (если был только 1)
    // ✓ Все фото привязаны к чекпоинтам
    // ✓ Store содержит все результаты
  });
});
```

#### Сценарий 04: Фото-워ークфлоу (40 мин)
```typescript
describe('Photo Workflow', () => {
  it('should upload and manage photos correctly', async () => {
    // 1. Открыть чекпоинт требующий фото
    // 2. Нажать "Add Photo"
    // 3. Выбрать 3 фотографии из галереи
    // ✓ PhotoGrid отображает все 3
    // 4. Перетащить фото для переупорядочивания
    // ✓ Порядок изменился
    // 5. Удалить одно фото (свайп)
    // ✓ Осталось 2 фото
    // 6. Сохранить
    // ✓ Фото привязаны к чекпоинту
  });
});
```

#### Сценарий 05: Экспорт в PDF (40 мин)
```typescript
describe('Export to PDF Workflow', () => {
  it('should export inspection to PDF with all photos', async () => {
    // 1. После завершения инспекции нажать "Export"
    // ✓ ExportProgressModal отображается
    // 2. Выбрать формат: "PDF Report"
    // 3. Нажать "Export"
    // ✓ Появляется progress bar
    // 4. Ожидание генерации PDF
    // ✓ PDF создан с фотографиями
    // ✓ Все чекпоинты включены в отчет
    // 5. Нажать "Share" или "Open"
    // ✓ PDF открывается или делится
  });
});
```

#### Сценарий 06: Участники (30 мин)
```typescript
describe('Participant Management', () => {
  it('should add/edit/delete participants', async () => {
    // ADD
    // 1. Нажать "Add Participant"
    // 2. Заполнить ParticipantForm
    // ✓ Добавлен в список

    // EDIT
    // 3. Нажать на участника
    // 4. Отредактировать ФИ
    // ✓ Сохранено

    // DELETE
    // 5. Нажать на меню (⋯)
    // 6. Выбрать "Delete"
    // ✓ Удален из списка
  });
});
```

#### Сценарий 07: Навигация (30 мин)
```typescript
describe('Navigation Flows', () => {
  it('should navigate between all screens correctly', async () => {
    // 1. Нажать Tab "Objects"
    // ✓ Видим список проектов

    // 2. Нажать на проект
    // ✓ Object Detail экран

    // 3. Нажать Tab "Maps"
    // ✓ Maps экран (функциональность)

    // 4. Нажать Tab "Services"
    // ✓ Services экран

    // 5. Deep linking: открыть /objects/[id]
    // ✓ Навигация работает
  });
});
```

#### Сценарий 08: Обработка ошибок (40 мин)
```typescript
describe('Error Handling', () => {
  it('should handle common errors gracefully', async () => {
    // Сценарий 1: Нет прав на фотогалерею
    // ✓ Показано сообщение об ошибке
    // ✓ Приложение не падает

    // Сценарий 2: Недостаточно места на диске
    // ✓ Ошибка при экспорте PDF
    // ✓ Пользователь предупрежден

    // Сценарий 3: Потеря данных (симуляция)
    // ✓ Восстановление из AsyncStorage
  });
});
```

#### Сценарий 09: Доступность (30 мин)
```typescript
describe('Accessibility (A11y)', () => {
  it('should support screen readers and accessibility features', async () => {
    // 1. Включить Voice Over (iOS) / TalkBack (Android)
    // ✓ Все элементы озвучены

    // 2. Навигация жестами доступности
    // ✓ Все экраны доступны

    // 3. Размер текста
    // ✓ Текст читаемый при любом размере

    // 4. Контрастность
    // ✓ Контрастность >= 4.5:1
  });
});
```

---

## ⚙️ ФАЗА 4: CI/CD ИНТЕГРАЦИЯ (2-3 дня)

### 4.1 GitHub Actions Workflows

**Файлы для создания:**

```
.github/workflows/
├── test-unit.yml ................. Unit тесты
├── test-integration.yml ......... Integration тесты
├── test-e2e.yml ................. E2E тесты (Detox)
└── build-and-test.yml .......... Полный pipeline
```

**Команды запуска:**
```bash
# Unit
npm run test:unit

# Integration
npm run test:integration

# E2E (требует simulator)
npm run detox:build
npm run detox:test

# Все вместе
npm run test:ci && npm run detox:test
```

### 4.2 Coverage требования
- **Unit**: 80%+ покрытие функций
- **Integration**: 70%+ основных flows
- **E2E**: 100% критических сценариев
- **Общее**: Минимум 75% покрытия кода

---

## 📚 ФАЗА 5: ДОКУМЕНТАЦИЯ (1-2 дня)

### Файлы документации:

```
docs/
├── TESTING_ROADMAP.md ........... Текущий файл - план реализации
├── TESTING_GUIDE.md ............ Практический гайд по написанию тестов
├── SCENARIO_TEMPLATE.md ........ Шаблон для новых сценариев
└── TEST_DATA.md ................ Описание тестовых данных и fixtures
```

---

## 📈 СВОДНАЯ ТАБЛИЦА: ГРАФИК РЕАЛИЗАЦИИ

| Фаза | Компонент | Объем | Сложность | Дни |
|------|-----------|-------|-----------|------|
| **0** | Инфраструктура | Jest, Detox, конфиги | 🟡 Средняя | 1-2 |
| **1** | Unit: Hooks | ~3 файла | 🟢 Низкая | 1 |
| **1** | Unit: Components | ~8 файлов | 🟢 Низкая | 1-2 |
| **1** | Unit: Services | ~3 файла | 🟢 Низкая | 1 |
| **2** | Integration: Flows | ~8 сценариев | 🟠 Высокая | 2-3 |
| **2** | Integration: Store | ~4 файла | 🟠 Высокая | 1-2 |
| **2** | Integration: Navigation | ~2 файла | 🟡 Средняя | 0.5-1 |
| **3** | E2E: Onboarding | 1 сценарий | 🟡 Средняя | 0.5 |
| **3** | E2E: Project Creation | 1 сценарий | 🟠 Высокая | 0.5 |
| **3** | E2E: Full Inspection | 1 сценарий | 🔴 Критичный | 1-2 |
| **3** | E2E: Photo Workflow | 1 сценарий | 🟠 Высокая | 0.5 |
| **3** | E2E: Export PDF | 1 сценарий | 🟠 Высокая | 0.5 |
| **3** | E2E: Participants | 1 сценарий | 🟡 Средняя | 0.5 |
| **3** | E2E: Navigation | 1 сценарий | 🟡 Средняя | 0.5 |
| **3** | E2E: Error Handling | 1 сценарий | 🟠 Высокая | 0.5 |
| **3** | E2E: Accessibility | 1 сценарий | 🟡 Средняя | 0.5 |
| **4** | CI/CD Integration | GitHub Actions | 🟡 Средняя | 1-2 |
| **5** | Документация | Гайды, шаблоны | 🟢 Низкая | 1 |
| | **ВСЕГО** | | | **17-25 дней** |

---

## 🚀 БЫСТРЫЙ СТАРТ

### Этап 1: Подготовка (сегодня)
```bash
# 1. Установить зависимости
npm install --save-dev jest @testing-library/react-native detox detox-cli

# 2. Инициализировать тесты
npm run test:unit -- --init    # Jest

# 3. Инициализировать Detox
detox init -r ios

# 4. Создать структуру папок
mkdir -p tests/{__mocks__,fixtures,hooks,components,services,integration}
mkdir -p e2e/{scenarios,pages,helpers}
mkdir -p .github/workflows
```

### Этап 2: Первые тесты (день 2-3)
```bash
# 1. Написать тесты для 2-3 hooks
npm run test:watch

# 2. Написать тесты для 2-3 компонентов

# 3. Запустить coverage
npm run test:coverage
```

### Этап 3: Integration тесты (день 4-5)
```bash
# 1. Написать flow тесты
npm run test:integration

# 2. Проверить store интеграцию
```

### Этап 4: E2E (день 6-7+)
```bash
# 1. Подготовить первый E2E сценарий
npm run detox:build
npm run detox:test:single -- --grep "onboarding"

# 2. Расширить сценарии
```

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ

### По завершении каждой фазы:
- ✅ Все тесты зеленые (passing)
- ✅ Coverage соответствует требованиям
- ✅ Code review пройден
- ✅ Документация обновлена
- ✅ Сценарии задокументированы

### Перед деплоем:
- ✅ Все unit тесты проходят
- ✅ Все integration тесты проходят
- ✅ Все критичные E2E сценарии проходят
- ✅ CI/CD pipeline зеленый
- ✅ Coverage >= 75%

---

## 🛠️ USEFUL COMMANDS

```bash
# Unit & Integration
npm test                              # Все тесты
npm run test:unit                    # Только unit
npm run test:integration             # Только integration
npm run test:watch                   # Watch mode
npm run test:coverage                # Coverage report
npm run test:ci                      # CI mode

# E2E (Detox)
npm run detox:build                  # Собрать app для тестов
npm run detox:test                   # Все E2E сценарии
npm run detox:test:single -- --grep "onboarding"  # Один сценарий

# Type checking
npm run type-check                   # TypeScript проверка

# CI/CD
git push origin <branch>             # Запустит GitHub Actions
```

---

## 📞 ПОДДЕРЖКА И ССЫЛКИ

- **Jest документация**: https://jestjs.io/docs/react-native
- **React Native Testing Library**: https://callstack.github.io/react-native-testing-library/
- **Detox**: https://wix.github.io/Detox/
- **Expo Testing**: https://docs.expo.dev/develop/testing/

---

## 📝 История изменений

| Дата | Версия | Изменения |
|------|--------|----------|
| 2025-12-19 | 1.0 | Создана первая версия дорожной карты |

---

**Статус**: 🔵 В разработке
**Последнее обновление**: 2025-12-19
**Ответственный**: Development Team
