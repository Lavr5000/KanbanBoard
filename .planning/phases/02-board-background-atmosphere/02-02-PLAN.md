---
phase: 02-board-background-atmosphere
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ui/background/GlowOrb.tsx
  - src/shared/ui/background/MouseParallax.tsx
  - src/shared/ui/background/BoardBackground.tsx
autonomous: true

must_haves:
  truths:
    - "Glowing orbs animate in on page load with staggered delays"
    - "Mouse movement creates subtle parallax effect on background"
    - "Parallax uses spring physics for smooth, responsive movement"
    - "Event listeners are properly cleaned up (no memory leaks)"
  artifacts:
    - path: "src/shared/ui/background/GlowOrb.tsx"
      provides: "Single glowing orb component with entrance animation"
      exports: ["GlowOrb"]
    - path: "src/shared/ui/background/MouseParallax.tsx"
      provides: "Mouse parallax wrapper with Framer Motion"
      exports: ["MouseParallax"]
    - path: "src/shared/ui/background/BoardBackground.tsx"
      provides: "Combined background wrapper component"
      exports: ["BoardBackground"]
  key_links:
    - from: "src/shared/ui/background/MouseParallax.tsx"
      to: "framer-motion"
      via: "useMotionValue, useSpring, useTransform"
      pattern: "useMotionValue|useSpring|useTransform"
    - from: "src/shared/ui/background/GlowOrb.tsx"
      to: "framer-motion"
      via: "motion.div for GPU-accelerated animations"
      pattern: "motion\\.div"
---

# 02-02: Glow Orbs and Mouse Parallax

## Objective

Create animated glow orbs and mouse parallax effect. These add depth and interactivity to the background, making the board feel alive and responsive to user movement.

**Purpose:** Add visual delight through ambient light effects that respond to user interaction.

**Output:** Three components providing glow effects and parallax movement.

## Context

@src/shared/ui/background/AnimatedGradient.tsx (created in 02-01)
@src/shared/ui/background/NoiseTexture.tsx (created in 02-01)
Framer Motion is already available in project dependencies

## Tasks

<task type="auto">
  <name>Create GlowOrb and MouseParallax components</name>
  <files>src/shared/ui/background/GlowOrb.tsx, src/shared/ui/background/MouseParallax.tsx, src/shared/ui/background/BoardBackground.tsx, src/shared/ui/background/index.ts</files>
  <action>
**File 1: GlowOrb.tsx**
```tsx
"use client";

import React from "react";
import { motion } from "framer-motion";

interface GlowOrbProps {
  size: number;
  color: string;
  top?: string;
  bottom?: string;
  left?: string;
  right?: string;
  delay?: number;
}

/**
 * Glowing orb with entrance animation.
 * Uses radial gradient for soft diffuse light effect.
 * Heavy blur (blur-3xl) creates ambient glow appearance.
 */
export function GlowOrb({ size, color, top, bottom, left, right, delay = 0 }: GlowOrbProps) {
  return (
    <motion.div
      className="rounded-full blur-3xl pointer-events-none -z-10 fixed"
      style={{
        width: `${size}px`,
        height: `${size}px`,
        background: `radial-gradient(circle, ${color}, transparent)`,
        top: top ?? 'auto',
        bottom: bottom ?? 'auto',
        left: left ?? 'auto',
        right: right ?? 'auto',
      }}
      initial={{ opacity: 0, scale: 0.8 }}
      animate={{ opacity: 1, scale: 1 }}
      transition={{ delay, duration: 1, ease: 'easeOut' }}
    />
  );
}
```

**File 2: MouseParallax.tsx**
```tsx
"use client";

import React, { useEffect } from "react";
import { motion, useMotionValue, useSpring, useTransform, MotionValue } from "framer-motion";
import { clsx } from "clsx";

interface MouseParallaxProps {
  children: React.ReactNode;
  strength?: number;
  className?: string;
  disabled?: boolean;
}

/**
 * Mouse parallax wrapper using Framer Motion.
 * Tracks mouse position and applies subtle offset to children.
 *
 * CRITICAL: Event listener cleanup in useEffect prevents memory leak.
 */
export function MouseParallax({ children, strength = 20, className = '', disabled = false }: MouseParallaxProps) {
  // Motion values for mouse position (normalized -0.5 to 0.5)
  const x = useMotionValue(0);
  const y = useMotionValue(0);

  // Spring config for smooth, responsive movement
  const springConfig = { stiffness: 300, damping: 30, mass: 0.8 };
  const springX = useSpring(x, springConfig);
  const springY = useSpring(y, springConfig);

  // Transform normalized mouse position to pixel offset
  const translateX = useTransform(springX, [-0.5, 0.5], [-strength, strength]);
  const translateY = useTransform(springY, [-0.5, 0.5], [-strength, strength]);

  // Set up mouse move listener with cleanup
  useEffect(() => {
    if (disabled || typeof window === 'undefined') return;

    const handleMouseMove = (e: MouseEvent) => {
      const normalizedX = e.clientX / window.innerWidth - 0.5;
      const normalizedY = e.clientY / window.innerHeight - 0.5;
      x.set(normalizedX);
      y.set(normalizedY);
    };

    window.addEventListener('mousemove', handleMouseMove);
    return () => window.removeEventListener('mousemove', handleMouseMove);
  }, [x, y, disabled]);

  return (
    <motion.div className={className} style={{ x: translateX, y: translateY }}>
      {children}
    </motion.div>
  );
}
```

**File 3: BoardBackground.tsx**
```tsx
"use client";

import React from "react";
import { AnimatedGradient } from "./AnimatedGradient";
import { NoiseTexture } from "./NoiseTexture";
import { GlowOrb } from "./GlowOrb";
import { MouseParallax } from "./MouseParallax";

interface BoardBackgroundProps {
  enableParallax?: boolean;
}

/**
 * Complete background system for the Kanban board.
 * Combines gradient, noise, glow orbs, and optional parallax.
 */
export function BoardBackground({ enableParallax = true }: BoardBackgroundProps) {
  const content = (
    <>
      {/* Base gradient mesh */}
      <AnimatedGradient />

      {/* Noise texture overlay */}
      <NoiseTexture />

      {/* Ambient glow orbs - positioned for depth */}
      <GlowOrb size={400} color="rgba(168, 85, 247, 0.3)" top="20%" left="20%" delay={0.2} />
      <GlowOrb size={300} color="rgba(59, 130, 246, 0.3)" bottom="30%" right="20%" delay={0.4} />
      <GlowOrb size={500} color="rgba(236, 72, 153, 0.2)" top="50%" left="50%" delay={0} />
      <GlowOrb size={250} color="rgba(34, 197, 94, 0.2)" top="70%" left="30%" delay={0.6} />
    </>
  );

  if (enableParallax) {
    return <MouseParallax strength={15}>{content}</MouseParallax>;
  }

  return <>{content}</>;
}
```

**Update index.ts** (append to existing exports):
```tsx
export { GlowOrb } from './GlowOrb';
export { MouseParallax } from './MouseParallax';
export { BoardBackground } from './BoardBackground';
```

Key implementation notes:
- MouseParallax uses useEffect with cleanup to prevent memory leak
- Spring config matches research recommendations
- Four glow orbs with varying sizes (250-500px) and colors
- Parallax strength of 15px for subtle effect
- All components marked "use client" for Next.js
  </action>
  <verify>
Files exist and TypeScript compiles: npx tsc --noEmit
Check for proper cleanup in MouseParallax useEffect
  </verify>
  <done>
All background components created with proper exports, memory leak prevented via useEffect cleanup
  </done>
</task>

## Verification

- [ ] `GlowOrb.tsx` uses motion.div with entrance animation
- [ ] `MouseParallax.tsx` has useEffect with cleanup function
- [ ] `BoardBackground.tsx` combines all background elements
- [ ] `index.ts` exports all 4 components (AnimatedGradient, NoiseTexture, GlowOrb, MouseParallax, BoardBackground)
- [ ] TypeScript compilation succeeds
- [ ] No linter warnings about missing dependencies

## Success Criteria

Background system is complete and ready for integration into Board component. All components follow Framer Motion best practices with proper cleanup.

## Output

After completion, create `.planning/phases/02-board-background-atmosphere/02-02-SUMMARY.md` with:
- Components created confirmation
- Memory leak prevention verified
- Any deviations from plan
