---
phase: 02-board-background-atmosphere
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/widgets/board/ui/Board.tsx
  - src/app/globals.css
autonomous: false

must_haves:
  truths:
    - "BoardBackground is visible behind the kanban board"
    - "Animated gradient shifts smoothly over 30 seconds"
    - "Glow orbs fade in on page load with staggered timing"
    - "Mouse movement creates subtle parallax effect"
    - "Noise texture adds subtle grain without distraction"
    - "Drag and drop functionality still works correctly"
  artifacts:
    - path: "src/widgets/board/ui/Board.tsx"
      provides: "Main board with integrated background"
      contains: "BoardBackground import and usage"
    - path: "src/app/globals.css"
      provides: "Updated body styles for background compatibility"
      contains: "body background styling"
  key_links:
    - from: "src/widgets/board/ui/Board.tsx"
      to: "src/shared/ui/background/BoardBackground"
      via: "import and render at component root"
      pattern: "import.*BoardBackground.*from.*background"
---

# 02-03: BoardBackground Integration

## Objective

Integrate the BoardBackground component into the main Board widget. Verify visual appearance and ensure drag-and-drop functionality remains intact.

**Purpose:** Make the atmospheric background visible and functional in the live application.

**Output:** Board component with integrated background, visual verification.

## Context

@src/shared/ui/background/BoardBackground.tsx (created in 02-02)
@src/widgets/board/ui/Board.tsx (main board component)
@src/app/globals.css (body has `#121218` background)

## Tasks

<task type="auto">
  <name>Integrate BoardBackground into Board component</name>
  <files>src/widgets/board/ui/Board.tsx</files>
  <action>
1. Add BoardBackground import at top of file (around line 31 with other imports):
```tsx
import { BoardBackground } from "@/shared/ui/background";
```

2. Update body background in globals.css (optional, for fallback):
Change from `background-color: #121218;` to `background: #121218;` on body.
This ensures the gradient has a dark base to work with.

3. Add BoardBackground component at the TOP of the Board component's return statement.
The Board component returns a main container - add BoardBackground as the first element inside.

Find the main return structure (around line 500+) and ensure BoardBackground is rendered:
```tsx
return (
  <>
    {/* Atmospheric background */}
    <BoardBackground enableParallax={!prefersReducedMotion} />

    {/* Existing board content */}
    <main className="...">
      {/* rest of existing content */}
    </main>
  </>
);
```

Key integration notes:
- BoardBackground goes first in JSX (behind everything due to z-index)
- Disable parallax if prefersReducedMotion is true
- Use fragment to avoid adding extra DOM wrapper
- Do NOT modify existing drag-and-drop logic
  </action>
  <verify>
Check Board.tsx has BoardBackground import and component rendered
  </verify>
  <done>
BoardBackground integrated at top of Board component return, parallax respects reduced motion preference
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete BoardBackground integration:
    - Gradient mesh background with 30s animation
    - 4 glow orbs with staggered entrance
    - Noise texture overlay for depth
    - Mouse parallax effect (15px strength)
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (in project directory)
    2. Open http://localhost:3000 in browser
    3. Verify background is visible (purple-blue gradient, not plain gray)
    4. Move mouse - background should shift slightly with parallax
    5. Wait and observe - glow orbs should fade in on page load
    6. Test drag and drop - pick up a task card, verify it works
    7. Check for console errors
  </how-to-verify>
  <resume-signal>Type "approved" if background looks correct and drag-drop works, or describe issues</resume-signal>
</task>

## Verification

- [ ] BoardBackground imported in Board.tsx
- [ ] BoardBackground rendered at top of component return
- [ ] Parallax disabled when prefersReducedMotion is true
- [ ] No TypeScript errors
- [ ] Dev server starts without errors
- [ ] Background visible in browser
- [ ] Drag-and-drop still functional

## Success Criteria

Board has atmospheric background with animated gradient, glow orbs, and parallax. Drag-and-drop functionality unaffected. Visual appearance enhances glassmorphism design.

## Output

After completion, create `.planning/phases/02-board-background-atmosphere/02-03-SUMMARY.md` with:
- Integration confirmation
- Visual verification results (from checkpoint)
- Any issues found and resolved
- Screenshot comparison (optional)
