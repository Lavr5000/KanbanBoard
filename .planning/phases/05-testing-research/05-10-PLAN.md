---
phase: 05-testing-research
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - test-stagger.png
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No references to framer-motion in codebase"
    - "test-stagger.png file removed (not needed, was test page for removed library)"
    - "npm run typecheck shows no framer-motion import errors"
  artifacts:
    removed:
      - path: "test-stagger.png"
        reason: "Leftover test artifact for framer-motion which was removed"
  key_links: []
---

<objective>
Clean up framer-motion references and test artifacts.

**Purpose:** Framer-motion was removed from the project (replaced with CSS due to React 19 incompatibility - see 05-CONTEXT.md Bug #3). The test-stagger.png file and any remaining references should be cleaned up.

**Output:** No framer-motion imports or artifacts remain in the project.
</objective>

<execution_context>
@C:\Users\user\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-research/05-VERIFICATION.md
@.planning/phases/05-testing-research/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Remove framer-motion test artifacts and check for remaining imports</name>
  <files>test-stagger.png</files>
  <action>
    1. Delete test-stagger.png (leftover test artifact for removed framer-motion library)
    2. Search for any remaining framer-motion imports in the codebase:
       - Run: `grep -r "framer-motion" src/` or use Grep tool
       - Run: `grep -r "from 'framer-motion'" .`
    3. If any imports found, note them but DO NOT modify - they may be in comments or documentation
    4. Only remove actual import statements from TypeScript/JavaScript files
  </action>
  <verify>
    Run: `npm run typecheck` - verify no framer-motion import errors
    Run: `npm run build` - verify build succeeds
  </verify>
  <done>
    - test-stagger.png removed
    - No actual framer-motion imports remain in source code
    - Typecheck and build pass
  </done>
</task>

</tasks>

<verification>
After completion:

1. Run `npm run typecheck` - verify no framer-motion errors
2. Run `npm run build` - verify production build works
3. Check test-stagger.png no longer exists
</verification>

<success_criteria>
- test-stagger.png removed
- No framer-motion imports in source code
- Build and typecheck pass without framer-motion errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-research/05-10-SUMMARY.md`
</output>
