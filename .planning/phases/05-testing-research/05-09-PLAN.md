---
phase: 05-testing-research
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - package.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TypeScript typecheck can run in pre-commit without @testing-library/jest-dom conflicts"
    - "Pre-commit hooks catch TypeScript syntax errors before commit"
    - "npm run typecheck passes for all files including tests"
  artifacts:
    - path: "package.json"
      provides: "lint-staged configuration with typecheck"
      has_tsc_noemit: true
    - path: "tsconfig.json"
      provides: "TypeScript configuration that excludes test type conflicts"
  key_links:
    - from: ".husky/pre-commit"
      to: "package.json"
      via: "lint-staged configuration"
      pattern: "lint-staged.*tsc"
---

<objective>
Restore TypeScript typecheck to pre-commit hooks by resolving @testing-library/jest-dom type conflicts.

**Purpose:** TypeScript typecheck was removed from lint-staged (05-01-SUMMARY.md deviation #2) due to @testing-library/jest-dom type conflicts. This means TypeScript syntax errors can reach commits. Need to either: (1) fix the type conflicts to re-enable tsc in pre-commit, or (2) document the limitation and rely on CI-only typecheck.

**Approach:** Create a separate tsconfig for tests that excludes problematic jest-dom types, allowing tsc --noEmit to run in pre-commit.

**Output:** Pre-commit hooks include TypeScript typecheck, catching syntax errors before commit.
</objective>

<execution_context>
@C:\Users\user\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-research/05-VERIFICATION.md
@.planning/phases/05-testing-research/05-01-SUMMARY.md
@.planning/phases/05-testing-research/05-CONTEXT.md
@tsconfig.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Create tsconfig for pre-commit typecheck that excludes test file type conflicts</name>
  <files>tsconfig.json</files>
  <action>
    1. Create a new tsconfig specifically for pre-commit: tsconfig.lint.json
    2. This config will:
       - Exclude test files from strict type checking OR
       - Use skipLibCheck more aggressively for jest-dom
       - Include a "types" array that excludes jest types

    Approach A - Exclude test files (simplest):
    ```json
    {
      "extends": "./tsconfig.json",
      "exclude": ["node_modules", "**/*.test.ts", "**/*.test.tsx", "**/__tests__/**"]
    }
    ```

    Approach B - Skip problematic lib types:
    Add to existing tsconfig.json compilerOptions:
    ```json
    "skipLibCheck": true  // Already set
    ```

    3. Update package.json scripts:
       Add "typecheck:strict" for full check (including tests)
       Keep "typecheck" for pre-commit (excludes tests or uses lax config)

    4. Update lint-staged in package.json:
       ```json
       "lint-staged": {
         "*.{ts,tsx}": [
           "eslint --fix",
           "tsc --noEmit --project tsconfig.lint.json"
         ]
       }
       ```

    This approach allows typechecking source code (where Bug #2 occurred) while avoiding jest-dom type conflicts in test files.
  </action>
  <verify>
    Run: `npm run typecheck` - should pass for src files
    Run: `git add . && npx lint-staged` - pre-commit should succeed
  </verify>
  <done>
    - tsconfig.lint.json created (excludes test files from typecheck)
    - package.json lint-staged includes tsc --noEmit
    - Pre-commit hook runs both ESLint and TypeScript check
    - No jest-dom type conflicts in pre-commit
  </done>
</task>

<task type="auto">
  <name>Update documentation for typecheck limitation</name>
  <files>
    TESTING.md
    .planning/phases/05-testing-research/05-CONTEXT.md
  </files>
  <action>
    1. Read TESTING.md if it exists
    2. Add a section documenting the typecheck approach:
       - Pre-commit typecheck excludes test files (due to jest-dom types)
       - Full typecheck (including tests) runs in CI only
       - Run `npm run typecheck:all` to check everything including tests
    3. If TESTING.md doesn't exist, create it with this documentation
  </action>
  <verify>
    File exists and documents the typecheck limitation clearly
  </verify>
  <done>
    - Documentation clearly explains pre-commit vs CI typecheck
    - Developer knows when to run which typecheck command
  </done>
</task>

</tasks>

<verification>
After completion:

1. Run `git add . && npx lint-staged` - verify pre-commit hook succeeds with typecheck
2. Run `npm run typecheck` - verify source files typecheck passes
3. Introduce a deliberate TypeScript error in src/ - verify pre-commit catches it
</verification>

<success_criteria>
- Pre-commit hooks include TypeScript typecheck for source files
- No @testing-library/jest-dom type conflicts in pre-commit
- TypeScript syntax errors in source code caught before commit
- Documentation explains the typecheck approach
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-research/05-09-SUMMARY.md`
</output>
