---
phase: 05-testing-research
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/fixtures/auth.ts
  - playwright.config.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Playwright fixtures have no circular type references"
    - "npm run typecheck passes for e2e fixtures"
    - "E2E tests still run successfully"
  artifacts:
    - path: "e2e/fixtures/auth.ts"
      provides: "Playwright authentication fixtures"
      no_circular_types: true
  key_links:
    - from: "e2e/fixtures/auth.ts"
      to: "playwright.config.ts"
      via: "fixtures import in test config"
      pattern: "fixtures.*auth"
---

<objective>
Fix circular type references in Playwright E2E fixtures.

**Purpose:** The e2e/fixtures/auth.ts file has circular type references causing TypeScript errors. This needs to be resolved for typecheck to pass on E2E test files.

**Output:** E2E fixtures have clean type definitions without circular references, typecheck passes.
</objective>

<execution_context>
@C:\Users\user\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-research/05-VERIFICATION.md
@.planning/phases/05-testing-research/05-01-SUMMARY.md
@e2e/fixtures/auth.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Fix circular type references in Playwright fixtures</name>
  <files>e2e/fixtures/auth.ts</files>
  <action>
    1. Read the current auth.ts file to identify the circular reference
    2. Common circular reference pattern in Playwright fixtures: test extends fixtures which extend test
    3. Fix by using proper fixture typing from @playwright/test

    Common fixes:
    - Use `type MyFixtures = { authFixture: typeof myAuthFixture }` instead of extending
    - Import `test as base` from '@playwright/test' and extend properly
    - Use `MyFixtures` interface in test.extend&lt;MyFixtures&gt;()

    If the circular reference is between multiple fixture files:
    - Consolidate into a single fixtures.ts file
    - Or use proper type exports with `export type` instead of `export interface`
  </action>
  <verify>
    Run: `npm run typecheck` - verify no circular reference errors for e2e/fixtures
    Run: `npm run test:e2e` - verify E2E tests still work
  </verify>
  <done>
    - No circular type reference errors in auth.ts
    - Typecheck passes for E2E fixtures
    - E2E tests run successfully
  </done>
</task>

</tasks>

<verification>
After completion:

1. Run `npm run typecheck` - verify no errors in e2e/fixtures
2. Run `npm run test:e2e` - verify E2E tests still pass
</verification>

<success_criteria>
- TypeScript typecheck passes for e2e/fixtures/auth.ts
- No circular reference warnings
- E2E tests execute without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-research/05-08-SUMMARY.md`
</output>
