---
phase: 05-testing-research
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/Column.unit.test.tsx
  - src/__tests__/TaskCard.unit.test.tsx
  - src/__tests__/EditTaskModal.unit.test.tsx
  - src/__tests__/Modal.unit.test.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Test data matches Task type definition (has status, type properties)"
    - "npm run typecheck passes for all unit test files"
    - "All tests still pass"
  artifacts:
    - path: "src/__tests__/Column.unit.test.tsx"
      provides: "Column component unit test"
      test_data_has: "status, type properties on Task objects"
    - path: "src/entities/task/model/types.ts"
      reference: "Task type requires status and type properties"
  key_links:
    - from: "src/__tests__/Column.unit.test.tsx"
      to: "src/entities/task/model/types.ts"
      via: "mockTasks array matches Task type"
      pattern: "status.*active.*type.*bug"
---

<objective>
Fix Task type mismatches in test data - add missing status and type properties.

**Purpose:** Test mock data in Column.test.tsx and other test files is missing required Task properties (status, type) as defined in src/entities/task/model/types.ts. This causes TypeScript errors when running typecheck.

**Output:** All test data objects match the Task type definition with all required properties.
</objective>

<execution_context>
@C:\Users\user\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-research/05-VERIFICATION.md
@.planning/phases/05-testing-research/05-01-SUMMARY.md
@src/entities/task/model/types.ts
@src/__tests__/Column.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Add missing status and type properties to Column test data</name>
  <files>src/__tests__/Column.unit.test.tsx</files>
  <action>
    Update mockTasks array (lines 11-28) to include missing required properties:

    Current mockTasks objects have: id, content, columnId, priority, createdAt, tags
    Required by Task type (src/entities/task/model/types.ts):
    - id: Id
    - columnId: Id
    - content: string
    - priority: "low" | "medium" | "high"
    - status: "active" | "waiting" | "paused"  (MISSING)
    - type: TaskType  (MISSING - "bug" | "feature" | "design" | "research")
    - tags: string[]
    - createdAt: string
    - assigneeId?: string

    Add to both mock task objects:
    - status: 'active' (default active state)
    - type: 'feature' (default feature type)

    Also import TaskType if needed for type safety.
  </action>
  <verify>
    Run: `npm run typecheck` - should show no errors for Column.test.tsx
    Run: `npm test -- Column` - test should still pass
  </verify>
  <done>
    - mockTasks array includes status and type properties
    - Typecheck passes for Column.unit.test.tsx
    - All 3 Column tests pass
  </done>
</task>

<task type="auto">
  <name>Fix Task type mismatches in other test files</name>
  <files>
    src/__tests__/TaskCard.unit.test.tsx
    src/__tests__/EditTaskModal.unit.test.tsx
    src/__tests__/Modal.unit.test.tsx
  </files>
  <action>
    For each test file that has Task mock data, add missing properties:

    1. Read each file and find Task mock objects
    2. Add missing properties: status, type
    3. Fix property name mismatches (e.g., column_id -> columnId if found in EditTaskModal test)
    4. Ensure type safety with correct property names

    Default values to add:
    - status: 'active'
    - type: 'feature' (or appropriate type for the test context)

    Check specifically for:
    - TaskCard.unit.test.tsx - verify mock tasks have all properties
    - EditTaskModal.unit.test.tsx - fix column_id to columnId if present, add status/type
    - Modal.unit.test.tsx - verify mock tasks have all properties
  </action>
  <verify>
    Run: `npm run typecheck` - verify no TypeScript errors in modified test files
    Run: `npm test` - verify all tests pass
  </verify>
  <done>
    - All test Task objects match Task type definition
    - No TypeScript errors in test files
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After completion:

1. Run `npm run typecheck` - verify no TypeScript errors in test files
2. Run `npm test` - verify all tests pass
3. Check that error count is reduced from 34
</verification>

<success_criteria>
- All test mock data matches Task type definition
- Typecheck passes for all unit test files
- All 83+ tests still pass
- TypeScript error count significantly reduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-research/05-07-SUMMARY.md`
</output>
