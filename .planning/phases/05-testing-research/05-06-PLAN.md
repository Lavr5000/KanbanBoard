---
phase: 05-testing-research
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/AddTaskModal.unit.test.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "BoardProvider import issue in AddTaskModal test is resolved"
    - "npm run typecheck passes for AddTaskModal.unit.test.tsx"
    - "Test still passes with vitest"
  artifacts:
    - path: "src/__tests__/AddTaskModal.unit.test.tsx"
      provides: "AddTaskModal component unit test"
      imports_correct:
        - "BoardContext (not BoardProvider which doesn't exist)"
        - "useBoardContext hook from same module"
  key_links:
    - from: "src/__tests__/AddTaskModal.unit.test.tsx"
      to: "src/widgets/board/model/BoardContext.tsx"
      via: "import { useBoardContext }"
      pattern: "useBoardContext"
---

<objective>
Fix BoardProvider import error in AddTaskModal unit test.

**Purpose:** The test imports `BoardProvider` from BoardContext.tsx, but this export doesn't exist. Only `BoardContext` and `useBoardContext` are exported. The test also mocks BoardProvider incorrectly. Since the component internally uses `useBoardContext`, we need to mock the hook, not a non-existent provider component.

**Output:** Test file imports correctly from BoardContext, typecheck passes, test still passes.
</objective>

<execution_context>
@C:\Users\user\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-testing-research/05-VERIFICATION.md
@.planning/phases/05-testing-research/05-01-SUMMARY.md
@.planning/phases/05-testing-research/05-CONTEXT.md
@src/widgets/board/model/BoardContext.tsx
@src/__tests__/AddTaskModal.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix BoardProvider import and mock in AddTaskModal test</name>
  <files>src/__tests__/AddTaskModal.unit.test.tsx</files>
  <action>
    1. Remove line 5: `import { BoardProvider } from '@/widgets/board/model/BoardContext'`
    2. Update the mock (lines 21-24) to mock `useBoardContext` instead of `BoardProvider`:

    Current (incorrect):
    ```typescript
    vi.mock('@/widgets/board/model/BoardContext', () => ({
      useBoardContext: () => mockBoardContext,
      BoardProvider: ({ children }: { children: React.ReactNode }) => children,
    }))
    ```

    Change to:
    ```typescript
    vi.mock('@/widgets/board/model/BoardContext', () => ({
      useBoardContext: () => mockBoardContext,
    }))
    ```

    The BoardProvider export doesn't exist in BoardContext.tsx (verified: only exports are BoardContext and useBoardContext). The test doesn't need a provider mock anyway - AddTaskModal uses useBoardContext hook internally.

    3. Verify the mockBoardContext object has all required properties for the component to work.
  </action>
  <verify>
    Run: `npm run typecheck` - should show no errors for this file
    Run: `npm test -- AddTaskModal` - test should still pass
  </verify>
  <done>
    - Import statement only imports existing exports from BoardContext
    - Typecheck passes for AddTaskModal.unit.test.tsx
    - All 11 AddTaskModal tests pass
  </done>
</task>

</tasks>

<verification>
After completion:

1. Run `npm run typecheck` - verify no TypeScript errors in AddTaskModal.test.tsx
2. Run `npm test -- AddTaskModal` - verify all tests pass
3. Run `npm test` - verify no regressions in other tests
</verification>

<success_criteria>
- TypeScript typecheck passes for AddTaskModal.unit.test.tsx
- All 11 AddTaskModal tests pass
- No new TypeScript errors introduced
- Total TypeScript error count reduced by at least 1
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-research/05-06-SUMMARY.md`
</output>
