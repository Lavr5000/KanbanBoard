# 🤖 Инструкция Передачи Работы между Сессиями ИИ

**Версия:** 1.0
**Дата создания:** 2025-12-19
**Для:** Локальное использование Claude или других ИИ с командной строкой

---

## 📋 О ЧЕМ ЭТОТ ДОКУМЕНТ

Это инструкция для **бесперебойной передачи работы между сессиями**, когда вы закрываете и открываете новое окно с ИИ.

**Проблема:**
- Каждое новое окно ИИ не знает контекст предыдущей работы
- Нужно каждый раз объяснять, что делалось и что делать дальше

**Решение:**
- Эта инструкция содержит всю информацию, которую нужно загрузить в ИИ
- ИИ сразу поймет, в какой точке работы находится проект
- ИИ сможет продолжить без потери контекста

---

## 🎯 КАК ИСПОЛЬЗОВАТЬ

### Шаг 1: Проверить статус проекта
```bash
# Выполнить в терминале
git status
git log --oneline -5
```

### Шаг 2: Загрузить инструкцию в новый ИИ
1. Открыть новое окно Claude Code / LodCode
2. Скопировать текст из этого файла (или ссылку на файл)
3. Вставить в чат ИИ с просьбой:
   ```
   "Используй эту инструкцию для продолжения работы над Kanban проектом"
   ```

### Шаг 3: Передать конкретную задачу
Например:
```
Прочитай TESTING_ENVIRONMENT_ROADMAP.md в папке проекта.
Реализуй ФАЗУ 1: Подготовка инфраструктуры (задачи 1.1 и 1.2)
Когда закончишь, коммитни код с сообщением "setup: configure vitest and playwright testing frameworks"
```

---

## 📂 СТРУКТУРА ПРОЕКТА

```
/home/user/0-KanBanDoska/
└── .claude/0 ProEKTi/kanban-board/        ← ОСНОВНАЯ ПАПКА ПРОЕКТА
    ├── src/
    │   ├── app/
    │   ├── features/
    │   │   └── kanban/
    │   │       ├── ui/
    │   │       │   ├── KanbanBoard.tsx
    │   │       │   ├── KanbanCard.tsx          ← ДА ИСПРАВИТЬ (БАГ #1)
    │   │       │   └── KanbanColumn.tsx
    │   │       └── hooks/
    │   └── shared/
    │       ├── store/
    │       │   └── kanbanStore.ts
    │       ├── types/
    │       │   └── task.ts
    │       └── ui/
    │           ├── PriorityBadge.tsx
    │           ├── DueDateIndicator.tsx
    │           └── ...
    ├── __tests__/                          ← ПАПКА ДЛЯ ТЕСТОВ (СОЗДАТЬ)
    │   ├── unit/
    │   ├── integration/
    │   ├── visual/
    │   └── e2e/
    ├── ROADMAP.md                          ← Дорожная карта (прочитано)
    ├── QUICK_START.md                      ← Быстрый старт (прочитано)
    ├── TESTING_ENVIRONMENT_ROADMAP.md      ← ТЕКУЩАЯ ДОРОЖНАЯ КАРТА ТЕСТИРОВАНИЯ
    ├── AI_HANDOFF_INSTRUCTIONS.md          ← ЭТА ИНСТРУКЦИЯ
    ├── package.json
    ├── next.config.js
    ├── tsconfig.json
    └── vitest.config.ts                    ← НУЖНО СОЗДАТЬ
```

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (НУЖНО ИСПРАВИТЬ)

### БАГ #1: Редактирование задачи закрывается при клике на приоритет
**Файл:** `src/features/kanban/ui/KanbanCard.tsx`
**Строки:** 87-182 (блок с `isEditing`)
**Причина:** `onBlur={() => setIsEditing(false)}` на текстовых полях (строки 95, 112)

**Проблема:**
1. User кликает на карточку → `setIsEditing(true)` ✅
2. Форма редактирования открывается ✅
3. User кликает на кнопку приоритета → textarea теряет фокус
4. `onBlur` срабатывает → `setIsEditing(false)`
5. Форма закрывается ❌ БАГ!

**Как исправить:**
```typescript
// ВАРИАНТ 1: Удалить onBlur
// Убрать: onBlur={() => setIsEditing(false)}
// Добавить кнопку закрытия

// ВАРИАНТ 2: Использовать mouseDown вместо onBlur
// Но это более сложно

// РЕКОМЕНДАЦИЯ: Вариант 1 проще и логичнее
```

---

### БАГ #2: Фильтрация не реализована
**Файл:** `src/features/kanban/ui/KanbanBoard.tsx`
**Проблема:** Нет UI компонентов и логики для фильтрации

**Что нужно:**
1. Создать компонент `src/shared/ui/FilterPanel.tsx`
2. Добавить состояние фильтров в `kanbanStore.ts`
3. Добавить методы фильтрации в store
4. Подключить `FilterPanel` к `KanbanBoard`
5. Реализовать фильтры по:
   - Приоритету
   - Статусу
   - Дате
   - Тексту (поиск)

**Структура FilterPanel:**
```typescript
export const FilterPanel = () => {
  // Состояние фильтров
  const { filters, setFilters } = useKanbanStore();

  return (
    <div className="flex gap-2 p-4">
      {/* Фильтр по приоритету */}
      {/* Фильтр по статусу */}
      {/* Кнопка сброса */}
    </div>
  );
};
```

---

## ✅ ЧТО УЖЕ РЕАЛИЗОВАНО

- ✅ React + Next.js 16.0.10 с TypeScript
- ✅ Drag & Drop (dnd-kit)
- ✅ Система приоритетов (4 уровня: urgent, high, medium, low)
- ✅ Система сроков (Due Dates) с визуальными индикаторами
- ✅ Темная/светлая тема
- ✅ Mock данные для демонстрации
- ✅ Zustand store с localStorage persistence
- ✅ 5 колонок: Todo → In Progress → Review → Testing → Done

---

## 🧪 ТЕКУЩИЙ ФОКУС: ТЕСТИРОВАНИЕ

**Дорожная карта:** `TESTING_ENVIRONMENT_ROADMAP.md` (в этой папке)

**Главные задачи:**
1. **ФАЗА 1 (3 дня):** Установить фреймворки (Vitest, React Testing Library, Playwright)
2. **ФАЗА 2 (10 дней):** Написать юнит и интеграционные тесты
3. **ФАЗА 3 (7 дней):** Visual и UI тесты
4. **ФАЗА 4 (10 дней):** E2E тесты
5. **ФАЗА 5 (5 дней):** CI/CD автоматизация

**Приоритет:**
1. ⚠️ Исправить БАГ #1 (редактирование)
2. ⚠️ Исправить БАГ #2 (фильтрация)
3. ✅ Установить тестовую инфраструктуру
4. ✅ Написать тесты

---

## 🔧 КОМАНДЫ ДЛЯ ЕЖЕДНЕВНОЙ РАБОТЫ

### Запуск приложения
```bash
cd /home/user/0-KanBanDoska/.claude/0\ ProEKTi/kanban-board
npm run dev
# Откроется на http://localhost:3000
```

### Запуск тестов (когда установлены)
```bash
npm run test:unit          # Юнит-тесты
npm run test:integration   # Интеграционные тесты
npm run test:e2e          # E2E тесты
npm test                  # Все тесты
```

### Git операции
```bash
# Текущая ветка: claude/kanban-board-setup-DOYSv
git status                 # Статус
git add .                  # Добавить все файлы
git commit -m "feat: ..."  # Коммит
git push -u origin claude/kanban-board-setup-DOYSv  # Push на ветку
```

### Просмотр структуры проекта
```bash
tree src -L 3  # Древо папок src
find src -name "*.tsx" | head -20  # Список всех компонентов
```

---

## 📖 ВАЖНЫЕ ФАЙЛЫ ДЛЯ ЧТЕНИЯ

В порядке приоритета:

### 1️⃣ КРИТИЧЕСКИЙ КОНТЕКСТ (обязательно прочитать)
- `TESTING_ENVIRONMENT_ROADMAP.md` ← Полная дорожная карта тестирования
- `src/features/kanban/ui/KanbanCard.tsx` ← Имеет БАГ #1
- `src/shared/store/kanbanStore.ts` ← Главное хранилище

### 2️⃣ СПРАВОЧНАЯ ИНФОРМАЦИЯ
- `ROADMAP.md` ← Полная дорожная карта продукта (11 фаз)
- `QUICK_START.md` ← Быстрый старт
- `src/shared/types/task.ts` ← Типы данных

### 3️⃣ КОМПОНЕНТЫ
- `src/features/kanban/ui/KanbanBoard.tsx` ← Главный компонент
- `src/features/kanban/ui/KanbanColumn.tsx` ← Колонна
- `src/shared/ui/PriorityBadge.tsx` ← Приоритеты
- `src/shared/ui/DueDateIndicator.tsx` ← Сроки

---

## 💾 КОГДА ЗАКОНЧИШЬ СЕССИЮ

### Перед закрытием окна:
1. Убедиться, что все изменения коммичены
2. Запустить команду:
```bash
git log --oneline -5
```
3. Скопировать последний хеш коммита
4. Сообщить в следующей сессии: "Последний коммит: ХЕШИРША"

### Передача в новую сессию:
```
Привет! Продолжаю работу над Kanban проектом.
Последний коммит: abc1234
Статус: Был написан код для ФАЗЫ X, ветка: claude/kanban-board-setup-DOYSv

Дальше нужно:
1. ...
2. ...
3. ...

Используй TESTING_ENVIRONMENT_ROADMAP.md для контекста.
```

---

## 🎓 ПРИМЕРЫ ЗАДАНИЙ ДЛЯ ИИ

### Пример 1: Исправление БАГ #1
```
Файл: src/features/kanban/ui/KanbanCard.tsx

ПРОБЛЕМА:
Когда user кликает на приоритет/дату в форме редактирования,
форма закрывается. Это происходит из-за onBlur обработчика.

ЗАДАЧА:
1. Прочитай строки 87-182 в KanbanCard.tsx
2. Удали "onBlur={() => setIsEditing(false)}" из текстовых полей
3. Добавь кнопку "Сохранить" и "Отмена" в форме
4. Тестируй: форма должна оставаться открытой при клике на приоритет/дату
5. Коммитни: "fix: allow editing all task fields without closing form"
```

### Пример 2: Реализация Фильтрации
```
ЗАДАЧА: Реализовать фильтр по приоритету

ПЛАН:
1. Создать компонент: src/shared/ui/FilterPanel.tsx
2. Добавить в kanbanStore.ts методы:
   - setFilter()
   - resetFilters()
   - getFilteredTasks()
3. Подключить FilterPanel к KanbanBoard.tsx
4. Написать юнит-тесты для фильтрации
5. Коммитни: "feat: add priority filter to kanban board"

Используй стиль темной темы (glass-morphism, Tailwind CSS)
Цвета приоритетов:
  - urgent: red
  - high: orange
  - medium: yellow
  - low: green
```

### Пример 3: Написание E2E теста
```
ЗАДАЧА: Написать E2E тест для редактирования задачи

ФАЙЛ: src/__tests__/e2e/task-editing.spec.ts

СЦЕНАРИЙ:
1. Открить приложение
2. Нажать на карточку → форма редактирования открывается
3. Изменить название → должно сохраняться
4. Изменить приоритет → форма остается открытой
5. Изменить дату → форма остается открытой
6. Нажать Enter → форма закрывается, все сохраняется
7. Перезагрузить страницу → все изменения остаются

Используй Playwright API:
- page.goto('/')
- page.click('[data-testid=...]')
- page.fill('[data-testid=...]', 'text')
- page.press(..., 'Enter')
- await expect(...).toBeVisible()
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

### GIT
- ✅ Всегда работай на ветке `claude/kanban-board-setup-DOYSv`
- ✅ Пушь только на эту ветку
- ✅ Каждое изменение - отдельный коммит с описанием
- ❌ НЕ пушь на main/master

### КОДИРОВАНИЕ
- ✅ TypeScript везде (никаких `any`)
- ✅ Следуй существующему стилю
- ✅ Используй Tailwind CSS для стилей
- ✅ Добавляй `data-testid` для тестируемых элементов
- ❌ НЕ изменяй конфиги без необходимости

### ТЕСТИРОВАНИЕ
- ✅ Пиши тесты для нового кода
- ✅ Запускай `npm test` перед commit
- ✅ Стремись к покрытию ≥ 80%
- ❌ НЕ коммитьте фейлящие тесты

---

## 📞 КОГДА ЧТО-ТО НЕ ПОНЯТНО

### Прочитай файлы в этом порядке:
1. `TESTING_ENVIRONMENT_ROADMAP.md` ← Для понимания, что тестировать
2. `ROADMAP.md` ← Для понимания архитектуры продукта
3. `src/shared/types/task.ts` ← Структура данных
4. `src/shared/store/kanbanStore.ts` ← Как работает store

### Примеры в коде:
- `src/features/kanban/ui/KanbanCard.tsx` ← Как писать компоненты React
- `src/shared/ui/PriorityBadge.tsx` ← Как писать переиспользуемые компоненты
- `src/features/kanban/hooks/useKanbanDnD.ts` ← Как писать кастомные hooks

---

## 🎯 ТЕКУЩИЙ ПРИОРИТЕТ РАБОТ

### 🔴 БЛОКИРУЮЩИЕ (нужно исправить первым)
1. БАГ #1: Редактирование закрывается
2. БАГ #2: Фильтрация не работает

### 🟠 ВАЖНЫЕ (нужно после багов)
1. Тестовая инфраструктура (Vitest, Playwright)
2. Юнит-тесты основных компонентов
3. E2E тесты критических сценариев

### 🟡 ЖЕЛАТЕЛЬНЫЕ (потом)
1. Visual regression тесты
2. Performance тесты
3. CI/CD в GitHub Actions

---

## ✨ ФИНАЛЬНАЯ ИНСТРУКЦИЯ

**Как использовать этот документ в следующий раз:**

1. **В новой сессии:**
   ```
   Привет! Продолжу работу над Kanban проектом.
   Прочитай файл: .claude/0 ProEKTi/kanban-board/AI_HANDOFF_INSTRUCTIONS.md
   Последний коммит: [ХЕШИРША КОММИТА]
   ```

2. **ИИ прочитает этот файл и поймет:**
   - Структуру проекта
   - Какие баги нужно исправить
   - Какие задачи выполнены
   - Какие задачи стоят дальше
   - Как работать с кодом

3. **Ты сразу скажешь, что дальше:**
   ```
   Дальше нужно:
   1. Исправить БАГ #1 (редактирование)
   2. Написать тесты для ...
   3. Реализовать ...
   ```

---

**Статус:** Готово к использованию
**Последнее обновление:** 2025-12-19
**Версия:** 1.0
