# Apartment Auditor

React Native приложение для инспекции квартир с помощью технологии AR.

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- Expo CLI
- React Native development environment
- **VPN подключение (обязательно для России)**

### Установка

```bash
# Установка зависимостей
npm install

# Запуск приложения с VPN туннелем
npx expo start --tunnel
```

> ⚠️ **Важно**: В России всегда используйте флаг `--tunnel` для VPN совместимости

## 🧪 Тестирование

### Доступные типы тестов

1. **Unit тесты**: Базовое тестирование компонентов
2. **Интеграционные тесты**: Тестирование воркфлоу
3. **E2E тесты**: Полное тестирование пользовательских сценариев с Detox

### Запуск тестов

```bash
# Все тесты
npm test

# Интеграционные тесты
npm run test:integration

# E2E тесты (требует настройки симуляторов)
npm run test:e2e

# TypeScript проверка
npm run type-check

# Smoke тест для проверки данных
npm run smoke-test
```

## 📱 E2E Тестирование

Приложение включает 9 E2E тестов для основных сценариев:

- Создание проекта
- Навигация по приложению
- Переключение режимов проверки
- Управление фотографиями
- Изменение статусов
- Прогресс категорий
- Выбор помещений
- Сохранение данных
- Завершение инспекции

Подробнее в [E2E_TESTING_GUIDE.md](./E2E_TESTING_GUIDE.md)

## 🏗️ Архитектура

- **Framework**: React Native 0.76+ с Expo SDK 54+
- **Navigation**: Expo Router (file-based routing)
- **State Management**: Zustand с persistence
- **Type Safety**: TypeScript
- **Testing**: Jest + Detox для E2E

## 📁 Структура проекта

```
apartment-app/
├── app/                 # Expo Router pages
├── components/          # React Native компоненты
├── services/           # Business logic and stores
├── constants/          # Константы и данные
├── types/              # TypeScript типы
├── tests/              # Unit и интеграционные тесты
├── e2e/                # E2E тесты (9 сценариев)
└── docs/               # Документация
```

## 🔧 Конфигурация

### ESM Совместимость

Проект использует `"type": "module"` в package.json. Все конфигурационные файлы используют расширение `.cjs`:

- `metro.config.cjs` - Metro bundler конфигурация
- `jest.config.cjs` - Jest конфигурация с jest-extended
- `detox.config.cjs` - Detox E2E тестирование

### VPN Совместимость

Все скрипты используют `--tunnel` флаг для работы через VPN:

```json
{
  "scripts": {
    "start": "expo start --tunnel",
    "dev": "expo start --tunnel --clear"
  }
}
```

## 📊 Данные

### Checkpoints Database

- **Файл**: `constants/checkpoints_v2.1.json`
- **Структура**: 383 чекпоинта, 10 категорий
- **Валидация**: Проверяется smoke-test'ом

## 🐛 Устранение проблем

### Частые проблемы

1. **Port 8081 is in use**
   ```bash
   npx expo start --tunnel --port 8082
   ```

2. **Metro configuration error**
   - Убедитесь что все `.js` конфигурационные файлы переименованы в `.cjs`

3. **Detox not working**
   - Проверьте установку detox-cli
   - Убедитесь что симуляторы настроены

4. **TypeScript errors**
   - Запустите `npm run type-check` для проверки
   - Основные файлы приложения должны компилироваться без ошибок

### Регулярное обслуживание

```bash
# Проверка целостности данных
npm run smoke-test

# Проверка типов
npm run type-check

# Очистка Metro кэша
npx expo start --tunnel --clear
```

## 📝 Лицензия

Private project

---

**Статус**: Production-ready для разработки
**Версия**: 1.0.0
**Последнее обновление**: 2025-12-19