# Todo: Динамические колонки для Kanban Board

## Задачи

- [x] 1. Изменить дефолтные колонки (5 → 4)
- [x] 2. Создать компонент ColumnHeader с inline редактированием
- [x] 3. Интегрировать ColumnHeader в Column.tsx
- [x] 4. Создать компонент AddColumnButton с лимитом 8
- [x] 5. Интегрировать AddColumnButton в Board.tsx
- [x] 6. Добавить удаление колонки с перемещением задач
- [x] 7. Тестирование всех операций

## Review

### ✅ Реализовано:

#### 1. Изменение дефолтных колонок
**Файл:** [`src/hooks/useBoardData.ts`](src/hooks/useBoardData.ts#L106-L111)
- Было: 5 колонок (Новая задача, Выполняется, Ожидает проверки, На тестировании, В доработку)
- Стало: 4 колонки (Новая задача, Выполняется, На тестировании, Выполнено)

#### 2. Компонент ColumnHeader
**Файл:** [`src/entities/column/ui/ColumnHeader.tsx`](src/entities/column/ui/ColumnHeader.tsx)
- Inline редактирование названия (клик → input → Enter/Escape)
- Иконка удаления (не показывается на первой колонке)
- Валидация: название не может быть пустым
- Кнопки сохранения/отмены при редактировании

#### 3. Интеграция ColumnHeader в Column
**Файл:** [`src/entities/column/ui/Column.tsx`](src/entities/column/ui/Column.tsx)
- Добавлены пропсы: `isFirst`, `onColumnUpdate`, `onColumnDelete`
- Заголовок заменён на компонент ColumnHeader

#### 4. Компонент AddColumnButton
**Файл:** [`src/features/add-column/ui/AddColumnButton.tsx`](src/features/add-column/ui/AddColumnButton.tsx)
- Кнопка добавления новой колонки
- Лимит 8 колонок с блокировкой
- При достижении лимита показывает сообщение "Максимум 8 колонок"

#### 5. Интеграция AddColumnButton в Board
**Файл:** [`src/widgets/board/ui/Board.tsx`](src/widgets/board/ui/Board.tsx)
- Добавлена кнопка после списка колонок
- Реализованы обработчики: `handleColumnUpdate`, `handleColumnDelete`

#### 6. Удаление колонок с перемещением задач
**Файл:** [`src/widgets/board/ui/Board.tsx`](src/widgets/board/ui/Board.tsx#L225-L254)
- При удалении колонки задачи перемещаются в первую колонку
- Нельзя удалить первую колонку
- Confirm dialog перед удалением

### 📁 Изменённые файлы:

| Файл | Тип изменений |
|------|---------------|
| `src/hooks/useBoardData.ts` | Изменён |
| `src/entities/column/ui/Column.tsx` | Изменён |
| `src/widgets/board/ui/Board.tsx` | Изменён |
| `src/entities/column/ui/ColumnHeader.tsx` | **Новый** |
| `src/features/add-column/ui/AddColumnButton.tsx` | **Новый** |

### 🎯 Функционал:

- ✅ Редактирование названия колонки (inline, Enter для сохранения)
- ✅ Добавление новых колонок (максимум 8)
- ✅ Удаление колонок (кроме первой)
- ✅ Перемещение задач при удалении колонки
- ✅ Блокировка добавления при достижении лимита
- ✅ Новые дефолтные колонки для новых досок

### ⚠️ Примечания:

- Все изменения используют существующие CRUD операции из Supabase
- Realtime обновления работают автоматически через Supabase subscriptions
- Сервер успешно компилируется без ошибок
