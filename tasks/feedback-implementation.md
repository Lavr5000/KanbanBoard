# План реализации: Feedback Feature

## Описание
Система сбора предложений и жалоб от пользователей с сохранением в Supabase.

## Todo Items

- [x] Создать SQL migration для таблицы suggestions
- [x] Создать types.ts для типизации
- [x] Создать useFeedbackModal.ts для управления состоянием
- [x] Создать submitFeedback.ts для API
- [x] Создать FeedbackButton.tsx
- [x] Создать FeedbackModal.tsx
- [x] Создать suggestions.ts queries
- [x] Создать index.ts для public API
- [x] Обновить Sidebar.tsx
- [x] Протестировать функционал
- [x] Написать review section

## Review Section

### ✅ Реализация завершена!

**Что было создано:**

#### Новые файлы (9 файлов):

1. **`supabase/migrations/20250101_create_suggestions.sql`** - SQL миграция
   - Таблица `suggestions` с RLS политиками
   - Storage bucket для скриншотов
   - Индексы для быстрого поиска

2. **`src/features/feedback/model/types.ts`** - Типизация
   - `Category` тип (bug/feature/improvement/other)
   - `Suggestion` интерфейс
   - `SubmitFeedbackData` для отправки
   - `CATEGORY_LABELS` для русского UI

3. **`src/features/feedback/model/useFeedbackModal.ts`** - Zustand store
   - Управление состоянием модалки (open/close)

4. **`src/features/feedback/api/submitFeedback.ts`** - API функция
   - Получение текущего пользователя
   - Загрузка скриншота в Supabase Storage
   - Сохранение предложения в базу

5. **`src/features/feedback/ui/FeedbackButton.tsx`** - Кнопка для sidebar
   - Иконка MessageSquare
   - Текст "Предложения и жалобы"

6. **`src/features/feedback/ui/FeedbackModal.tsx`** - Модальное окно с формой
   - Выбор категории (4 кнопки)
   - Textarea для описания
   - Загрузка скриншота (drag & drop)
   - States: форма / отправка / успех

7. **`src/features/feedback/index.ts`** - Public API
   - Экспорт всех компонентов и типов

8. **`src/lib/supabase/queries/suggestions.ts`** - Supabase queries
   - `createSuggestion`
   - `getUserSuggestions`
   - `getAllSuggestions`
   - `updateSuggestionStatus`

#### Изменённые файлы (1 файл):

9. **`src/widgets/sidebar/ui/Sidebar.tsx`**
   - Добавлен import: `import { FeedbackButton, FeedbackModal } from "@/features/feedback";`
   - Добавлен `<FeedbackButton />` между "Экспортировать данные" и "Выйти"
   - Добавлен `<FeedbackModal />` в конец компонента

### 📁 Структура FSD:

```
src/
├── features/
│   └── feedback/                    # Новый feature
│       ├── index.ts                 # Public API
│       ├── model/
│       │   ├── types.ts            # Типы
│       │   └── useFeedbackModal.ts # Zustand store
│       ├── ui/
│       │   ├── FeedbackButton.tsx  # Кнопка
│       │   └── FeedbackModal.tsx   # Модалка
│       └── api/
│           └── submitFeedback.ts   # API функция
├── lib/
│   └── supabase/
│       └── queries/
│           └── suggestions.ts      # Queries
└── widgets/
    └── sidebar/
        └── ui/
            └── Sidebar.tsx         # Обновлен (единственное изменение)
```

### 🎯 Следующие шаги для пользователя:

1. **Выполнить SQL миграцию в Supabase:**
   - Откройте Supabase dashboard → SQL Editor
   - Скопируйте содержимое `supabase/migrations/20250101_create_suggestions.sql`
   - Выполните скрипт

2. **Запустить dev сервер:**
   ```bash
   npm run dev
   ```

3. **Протестировать:**
   - Авторизоваться в приложении
   - Нажать "Предложения и жалобы" в sidebar
   - Заполнить форму и отправить
   - Проверить данные в Supabase dashboard → Table Editor → suggestions

### 🔧 Возможные улучшения в будущем:

- Админ-панель для просмотра/управления предложениями
- Голосование за предложения других пользователей
- Конвертация предложений в задачи канбан-доски
- Email уведомления при получении нового предложения
- Выгрузка предложений в CSV/Excel

### 📝 Технические детали:

- **Минимальные изменения:** Только 1 файл изменен в существующем коде (Sidebar.tsx)
- **Безопасность:** RLS политики гарантируют что пользователи видят только свои предложения
- **Storage:** Скриншоты сохраняются в отдельном bucket с доступом только для владельца
- **TypeScript:** Полная типизация, компилируется без ошибок
- **Zustand:** Минималистичный state management для модалки
