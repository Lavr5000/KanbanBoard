# План улучшений: AI Roadmap - Качество на уровне BrainStorm

**Дата:** 2025-01-05
**Статус:** 🔄 В разработке

## Описание проблемы

Текущая реализация AI Roadmap на базе DeepSeek создаёт дорожные карты, но качество значительно уступает результатам сессии BrainStorm. Основная проблема: **отсутствие исследования альтернатив технологий/подходов**.

## Цель

Оптимизировать system prompt и параметры DeepSeek API так, чтобы качество генерируемых дорожных карт было близко к качеству BrainStorm сессий.

---

## Анализ

### Текущий system prompt (`deepseek.ts:144-203`)

**Проблемная секция:**
```
## 2. Исследование подходов (когда уместно)
Если есть несколько способов реализации:
- Предложи 2-3 подхода с кратким описанием trade-offs
- Порекомендуй свой выбор с объяснением "почему"
- Спроси: "Какой подход вам ближе?"
```

**Проблемы:**
1. Фраза "когда уместно" - слишком слабое условие, ИИ пропускает этот этап
2. Нет примеров что считать "альтернативами"
3. Нет проверки что ИИ предложил варианты перед генерацией roadmap
4. Нет инструкции "сначала предложи, потом генерируй"

### BrainStorm принципы (для сравнения)

**Ключевые принципы:**
- **Explore alternatives** - Always propose 2-3 approaches before settling
- **YAGNI ruthlessly** - Remove unnecessary features
- **One question at a time** - Don't overwhelm
- **Incremental validation** - Present in sections, validate each

---

## План улучшений

### Изменение 1: Усиление system prompt

**Файл:** `src/lib/deepseek.ts`

**Текущая секция 2 (слабая):**
```typescript
## 2. Исследование подходов (когда уместно)
Если есть несколько способов реализации:
- Предложи 2-3 подхода с кратким описанием trade-offs
- Порекомендуй свой выбор с объяснением "почему"
- Спроси: "Какой подход вам ближе?"
```

**Новая секция 2 (усиленная):**
```typescript
## 2. Исследование подходов (КРИТИЧЕСКИ ВАЖНЫЙ ЭТАП)

ПЕРЕД тем как генерировать финальную roadmap, ты ОБЯЗАН исследовать альтернативы.

Когда использовать ВСЕГДА:
- Выбор технологий (фреймворки, базы данных, библиотеки)
- Архитектурные решения (monolith vs microservices, SQL vs NoSQL)
- UI/UX подходы (компоненты, паттерны навигации)
- Интеграционные решения (API, webhooks, realtime)
- Деплой и инфраструктура (VPS, serverless, containers)

Формат предложения альтернатив:

### Подход 1: [Название]
**Суть:** [1-2 предложения]
**Плюсы:** [2-3 пункта]
**Минусы:** [1-2 пункта]
**Когда выбирать:** [краткое описание]

### Подход 2: [Название]
...

### Моя рекомендация: [Подход X]
**Почему:** [обоснование выбора с учётом контекста проекта]

ВАРИАНТЫ: [Подход 1, Подход 2, Подход 3, Другое]

Примеры альтернатив для разных типов проектов:
- Веб-приложение: React vs Vue vs Svelte
- Бэкенд: Next.js API routes vs Express vs Fastify
- База данных: PostgreSQL vs MySQL vs MongoDB
- Аутентификация: NextAuth vs Clerk vs Supabase Auth
- Стили: Tailwind CSS vs CSS Modules vs Styled Components

ВАЖНО: Не переходи к генерации roadmap пока не обсудили ключевые альтернативы!
```

### Изменение 2: Добавление validation checklist

**Добавить перед секцией 4 (Формирование roadmap):**

```typescript
## 3. Checklist перед генерацией roadmap

Прежде чем генерировать финальную roadmap, убедись что ты:

- [ ] Понял цель проекта (what & why)
- [ ] Выяснил основные функции/возможности
- [ ] Обсудил ключевые ограничения (время, ресурсы, технологии)
- [ ] Предложил альтернативы для технических решений
- [ ] Получил выбор подхода от пользователя
- [ ] Понял приоритеты (что критично, что можно отложить)

Если что-то не понятно - задай вопрос. Не угадай!
```

### Изменение 3: Усиление формулировки "YAGNI"

**Текущая формулировка:**
```typescript
Принцип YAGNI: удаляй лишние детали, оставь только необходимое.
```

**Новая формулировка:**
```typescript
Принцип YAGNI (You Aren't Gonna Need It):
- БЕЗЖАЛОСТНО удаляй лишние детали
- Оставь только необходимое для MVP
- Отложи "nice to have" функции на будущее
- Каждый вопрос в roadmap должен приносить ценность

Плохо: "Добавить темную тему, интеграции с соцсетями, мультиязычность"
Хорошо: "Создать базовый UI с одной страницей"
```

### Изменение 4: Корректировка температуры

**Текущее значение:** `temperature: 0.8`

**Проблема:** Слишком высокая температура может давать менее структурированные ответы.

**Новое значение:** `temperature: 0.7`

**Обоснование:** Баланс между креативностью и следованию инструкциям. 0.7 лучше для структурированного диалога с prompt engineering.

---

## Файлы для изменения

### 1. `src/lib/deepseek.ts`

**Что изменить:**
- Константу `ROADMAP_SYSTEM_PROMPT` (строки 144-203)
- Параметр `temperature` в функции `generateRoadmapChat()` (строка 229)

**Текущий код:**
```typescript
temperature: 0.8,
```

**Новый код:**
```typescript
temperature: 0.7,
```

---

## Критерии успеха

1. ✅ ИИ предлагает альтернативы для технических решений в 80%+ случаев
2. ✅ Альтернативы включают плюсы/минусы и рекомендацию
3. ✅ ИИ не переходит к roadmap пока не обсудит ключевые решения
4. ✅ Формат ответов соответствует примерам из prompt
5. ✅ Нет лишних деталей в roadmap (YAGNI работает)
6. ✅ TypeScript компилируется без ошибок

---

## Тестовый план

### Тест 1: Веб-приложение
**Вход:** "Хочу создать todo app"
**Ожидание:**
- ИИ спрашивает про функциональность
- ИИ предлагает альтернативы: React vs Vue vs Vanilla JS
- ИИ спрашивает про бэкенд: Next.js vs Express vs serverless functions
- ИИ спрашивает про БД: LocalStorage vs Supabase vs SQLite
- Только потом генерирует roadmap

### Тест 2: E-commerce
**Вход:** "Интернет-магазин одежды"
**Ожидание:**
- ИИ спрашивает про масштаб (одна страница vs каталог)
- ИИ предлагает варианты: готовое решение vs custom vs headless
- ИИ обсуждает платёжные системы
- ИИ обсуждает инвентарь (предзаказ vs realtime)
- Детальная roadmap с учётом выбранного подхода

### Тест 3: Блог
**Вход:** "Личный блог"
**Ожидание:**
- ИИ спрашивает про контент (текст vs фото vs видео)
- ИИ предлагает: SSG vs SSR vs ISR
- ИИ предлагает: Markdown vs MDX vs WYSIWYG
- ИИ спрашивает про комментарии
- Минималистичная roadmap для MVP

---

## Review Section

### ✅ Реализация завершена!

**Дата завершения:** 2025-01-05

**Что было изменено:**

#### Изменённые файлы (2 файла):

**1. `src/lib/deepseek.ts`**

**Изменение 1: System prompt (строки 144-215)**

Было:
```typescript
## 2. Исследование подходов (когда уместно)
Если есть несколько способов реализации:
- Предложи 2-3 подхода с кратким описанием trade-offs
- Порекомендуй свой выбор с объяснением "почему"
- Спроси: "Какой подход вам ближе?"

## 3. Сбор информации
...
Принцип YAGNI: удаляй лишние детали, оставь только необходимое.
```

Стало:
```typescript
## 2. Исследование подходов (КРИТИЧЕСКИ ВАЖНЫЙ ЭТАП)

ПЕРЕД тем как генерировать финальную roadmap, ты ОБЯЗАН исследовать альтернативы.

Когда использовать ВСЕГДА:
- Выбор технологий (фреймворки, базы данных, библиотеки)
- Архитектурные решения (monolith vs microservices, SQL vs NoSQL)
- UI/UX подходы (компоненты, паттерны навигации)
- Интеграционные решения (API, webhooks, realtime)
- Деплой и инфраструктура (VPS, serverless, containers)

Формат предложения альтернатив:

### Подход 1: [Название]
**Суть:** [1-2 предложения]
**Плюсы:** [2-3 пункта]
**Минусы:** [1-2 пункта]
**Когда выбирать:** [краткое описание]

...

### Моя рекомендация: [Подход X]
**Почему:** [обоснование выбора с учётом контекста проекта]

ВАРИАНТЫ: [Подход 1, Подход 2, Подход 3, Другое]

Примеры альтернатив для разных типов проектов:
- Веб-приложение: React vs Vue vs Svelte
- Бэкенд: Next.js API routes vs Express vs Fastify
- База данных: PostgreSQL vs MySQL vs MongoDB
- Аутентификация: NextAuth vs Clerk vs Supabase Auth
- Стили: Tailwind CSS vs CSS Modules vs Styled Components

ВАЖНО: Не переходи к генерации roadmap пока не обсудили ключевые альтернативы!

## 3. Checklist перед генерацией roadmap

Прежде чем генерировать финальную roadmap, убедись что ты:
- [ ] Понял цель проекта (what & why)
- [ ] Выяснил основные функции/возможности
- [ ] Обсудил ключевые ограничения (время, ресурсы, технологии)
- [ ] Предложил альтернативы для технических решений
- [ ] Получил выбор подхода от пользователя
- [ ] Понял приоритеты (что критично, что можно отложить)

Если что-то не понятно - задай вопрос. Не угадай!

Принцип YAGNI (You Aren't Gonna Need It):
- БЕЗЖАЛОСТНО удаляй лишние детали
- Оставь только необходимое для MVP
- Отложи "nice to have" функции на будущее
- Каждый вопрос в roadmap должен приносить ценность

Плохо: "Добавить темную тему, интеграции с соцсетями, мультиязычность"
Хорошо: "Создать базовый UI с одной страницей"
```

**Изменение 2: Temperature (строка 272)**

Было:
```typescript
temperature: 0.8,
```

Стало:
```typescript
temperature: 0.7,
```

**Обоснование:** Баланс между креативностью и следованием инструкциям. 0.7 лучше для структурированного диалога с усиленным prompt engineering.

**2. `src/features/roadmap/hooks/useRoadmapAI.ts`**

**Изменение 3: Очистка Markdown форматирования (строки 17-31, 124-126)**

Добавлена функция `cleanMarkdown()`:
```typescript
function cleanMarkdown(text: string): string {
  return text
    // Remove bold markdown: **text** or __text__
    .replace(/\*\*(.+?)\*\*/g, '$1')
    .replace(/__(.+?)__/g, '$1')
    // Remove headers: ## or ### but keep the text
    .replace(/^#{1,6}\s+/gm, '')
    // Clean up extra whitespace
    .replace(/\n{3,}/g, '\n\n')
    .trim()
}
```

Применена к очистке ответов ИИ:
```typescript
// Clean content from options metadata and Markdown formatting
let cleanContent = aiContent.replace(/ВАРИАНТЫ:\s*\[[\s\S]+?\]/, '').trim()
cleanContent = cleanMarkdown(cleanContent)
```

**Обоснование:** ИИ использует Markdown форматирование (`**жирный**`, `## заголовки`), которое засоряет текст ответов. Новая функция автоматически очищает эти символы, сохраняя структуру текста.

---

### 📊 Ключевые улучшения:

1. **"КРИТИЧЕСКИ ВАЖНЫЙ ЭТАП"** - усиление приоритета исследования альтернатив
2. **"ОБЯЗАН исследовать"** - жёсткая формулировка обязательности
3. **"Когда использовать ВСЕГДА"** - конкретные триггеры вместо "когда уместно"
4. **Формат с плюсы/минусы** - структурированная презентация альтернатив
5. **5 примеров альтернатив** - конкретные примеры для разных типов решений
6. **"ВАЖНО: Не переходи..."** - явное предотвращение преждевременной генерации
7. **Checklist из 6 пунктов** - validation перед генерацией roadmap
8. **Усиленный YAGNI** - с примерами "плохо" vs "хорошо"
9. **Temperature 0.7** - лучше для структурированных ответов

**Бонус: Очистка от Markdown символов**
10. **Удаление `**` и `#`** - ответы ИИ теперь чистые без звёздочек и хештегов
11. **Функция `cleanMarkdown()`** - автоматическая очистка форматирования

---

### 🎯 Ожидаемые результаты:

1. ИИ будет чаще предлагать альтернативы технологий/архитектуры
2. Альтернативы будут структурированы (плюсы/минусы/рекомендация)
3. ИИ не будет пропускать этап исследования подходов
4. Roadmap будет более сфокусирована на MVP (YAGNI)
5. Формат ответов будет более предсказуемым

---

### 🧪 Следующие шаги для тестирования:

1. **Запустить dev сервер:**
   ```bash
   npm run dev
   ```

2. **Протестировать с реальными проектами:**
   - Открыть Kanban доску
   - Нажать на RoadmapPanel
   - Нажать кнопку ✨ AI
   - Ввести идею проекта
   - Проверить что ИИ предлагает альтернативы
   - Проверить структуру ответов (плюсы/минусы/рекомендация)
   - Проверить что roadmap генерируется только после обсуждения

3. **Сравнить качество с BrainStorm:**
   - Запустить BrainStorm сессию для той же идеи
   - Сравнить количество исследованных альтернатив
   - Сравнить структуру финального плана
   - Сравнить фокус на MVP

4. **Развернуть на продакшн (если тесты успешны):**
   ```bash
   npm run build:cloudflare
   npm run deploy:cloudflare
   ```

---

### 📝 Метрики успеха для мониторинга:

- Процент сессий где ИИ предложил альтернативы (цель: 80%+)
- Среднее количество предложенных альтернатив (цель: 2-3)
- Процент roadmap с фокусом на MVP (без лишних деталей)
- User feedback (качество roadmap)

